# Installed dependencies and generated build output (recreate with npm ci/build)
node_modules/
.worktrees/
dist/
*.tsbuildinfo

# Local environment; keep the documented example
.env
.env.*
!.env.example

# Local service links and tool caches
.vercel/
backend/supabase/.temp/
.overnight/

# Finder metadata and legacy generated server
.DS_Store
server.js
