# 유미당 프로젝트 작업 지침

## 최신 기준과 문서 작성

- 문서는 한국어와 필요한 영어로 작성한다. 한자·일본어 등 다른 언어가 잘못 섞이지 않았는지 변경 내용을 확인한다.
- 최신 서비스·백엔드 계획은 [PLAN.md](PLAN.md), 파일별 책임·AI 구조·연결 계약은 [PLAN_상세설계.md](PLAN_상세설계.md)를 따른다.
- 화면 배치는 [IA.md](docs/planning/design/IA.md), 행동·예외·복귀는 [USER_FLOW.md](docs/planning/design/USER_FLOW.md)를 확인한다. AI 요약은 한마디 있는 공개 후기 3개부터이며 한마디 없는 평가·비공개 후기는 제외한다. AI 대화는 현재 탐색 중만 유지하고 서비스 서버의 이력·오류·사용량 로그에 원문을 저장하지 않는다. 두 항목은 `PLAN.md` 6장의 사용자 확정으로, IA·USER_FLOW의 남은 미정 문구보다 우선한다. 외부 AI 업체의 로그 보관 조건은 별도 확인한다.
- **2026-09-23 추가 사용자 결정:** 수동 완료는 두 명 모두 확인해야 성립한다. 한 명만 확인하면 완료 전 상태를 유지하며 미처리 시 예상 종료 +24시간 자동 완료는 유지한다. 취소·불발·분쟁은 자동 완료에서 제외한다. 상세 설계 5.5·9·11.3장에 반영했으며 기존 SQL의 한 명 완료 방식과 최신 계획의 모호한 문구보다 이 결정을 우선한다.
- 신규 가입은 PASS 자격·DI 중복 확인, 필수 사진·선택 성향을 기준으로 하며 수기 입력 가입을 추가하지 않는다. 일반 로그인은 문자 일회용 코드, 번호 변경·복구·재가입은 PASS 재인증으로 구분한다.
- 공고 검색 대상은 제목·등록 장소명·등록 주소지만 검색 일치와 표시 권한은 별개다. 확정 전에는 공개 지역·마스킹 이름, 양쪽 확정 후 당사자에게 전체 실명·정확한 주소·상세 지점을 제공한다. 비로그인은 시스템 별칭을 사용한다. 상세 만남 지점은 검색·AI 입력에서 제외하고 주변·반경·거리순 검색을 추가하지 않는다.
- 유료 등록·신청 때 계좌 인증을 확인하고 인증 후 입력을 보존해 직접 재제출하게 한다. 최종 동의에 추가 S22 인증 단계를 넣지 않는다. 등록·신청 후 계좌 효력 변경 시 확정 허용 여부는 미정이다. 직접 지급·플랫폼 결제/대금 보관/정산/자동 환불 제외와 무효 계좌 배지 숨김은 확정 기준이다.
- 완료·평가 시간과 행사 신규/진행 중/작성 선택 모드는 최신 계획과 상세 설계 5.5·8장을 따른다. 서비스 정책을 워커·AI가 임의로 결정하지 않는다. 새 충돌이나 미정 선택은 사용자에게 확인하고 확정 정책·기술 제안·실제 검증 결과를 구분한다.

## 팀 작업

Always use harness agent team architecture for complex tasks.

## 동시 작업: 파일별 단독 수정

- 수정 전에 [소유권 정책](backend/ownership.json)과 [협업 규칙](docs/collaboration/README.md)을 읽는다. 현재 대화에서 확인한 작업자를 유지한다. 민규는 `minkyu`, 종현은 `jonghyun`이다. 작업자를 모르면 읽기·계획을 먼저 진행하고 수정 전에 담당자를 확인한다.
- 각 파일은 지정된 한 사람만 수정한다. 상대 담당 파일은 읽거나 검토할 수 있지만 직접 수정·삭제·이동·포맷하지 않는다. 에이전트 팀에도 같은 작업자와 수정 허용 경로를 전달한다.
- 편집 전 대상 경로를 `python3 tools/collaboration/check_ownership.py --actor 작업자 --paths 경로...`로 확인한다. 실패한 경로는 수정 대상에서 제외하고 본인 담당 작업을 계속한다. 커밋 전에는 `--staged`로 확인한다.
- 상대 담당 변경은 `docs/collaboration/requests/minkyu/` 또는 `requests/jonghyun/` 중 자기 폴더에 문서로 작성한다. 상대가 작성한 요청·현황 파일을 수정하지 않는다. 답변은 자기 폴더에 새 문서로 작성해 연결한다. 명시적으로 요청받지 않은 외부 메시지는 보내지 않는다.
- 공통 설정·인증·DB 클라이언트·마이그레이션·소유권 정책·최상위 문서와 지침은 민규만 편집한다. 종현은 AI·검색·행사·작업 처리 영역을 편집한다. 세부 예외는 소유권 정책의 가장 구체적인 경로가 기준이다.
- 검색 문서는 종현 소유 `backend/contracts/search.md`에 작성한다. 민규 소유 `posts-search.md`를 공동 편집하지 않는다. 테스트는 `tests/분류/작업자/`, seed는 `backend/supabase/seeds/작업자/`에 작성한다.
- 각각 별도의 clone/worktree·브랜치에서 작업하고 같은 작업 디렉터리나 Git 인덱스를 공유하지 않는다. 전체 저장소 포맷·자동 수정·대량 스테이징으로 다른 담당 파일을 건드리지 않는다.
- 소유권 정책·hook·작업자 설정을 바꿔 자신의 변경을 통과시키지 않는다. 역할 재배정이 명시적으로 합의되면 민규가 정책을 별도 변경하고 양쪽이 갱신한 후 작업한다. 자동 커밋·푸시를 의미하지 않는다.

## 백엔드 구현 파일 작성 위치

- 백엔드 작업 전에 `PLAN.md`의 최신 정책, `backend/README.md`의 기능별 작성 위치와 `PLAN_상세설계.md` 11장의 담당·연결 계약을 확인한다. 기존 안내·계약·골격 TODO에 이전 정책이 남아 있으면 최신 정책과 이 지침의 추가 사용자 결정을 우선하고 담당별 파일에서 동기화한다. 과거 수기 가입·기본 주소 공개·최종 확정 계좌 인증 강제를 그대로 구현하지 않는다.
- 설계된 구현용 파일은 이미 생성했다. 이후 기능 구현은 해당 파일의 역할·담당·TODO에 따라 기존 파일을 채우는 방식으로 진행한다. 같은 기능을 담는 별도 백엔드·AI 폴더나 이름만 다른 중복 파일을 만들지 않는다.
- AI 탐색은 `backend/supabase/functions/_shared/ai/Agents/chatbot/`, 리뷰 요약은 `ai/Agents/review-summary/`, 모델 연결은 `ai/providers/`에 작성한다. HTTP 진입점은 각각 `functions/ai-chat/`, `functions/review-summary-worker/`다. 여기서 생략된 경로는 같은 `backend/supabase/functions/` 및 `_shared/` 아래를 뜻한다.
- `PLAN.md`의 간략한 AI 폴더 트리를 이유로 위 실제 작성 위치를 다시 이동하거나 `ai/chatbot/`·`ai/review-summary/`에 중복 파일을 만들지 않는다. 공개 검색·장소·우편번호 주소 입력·행사·예약 처리도 기존 담당 모듈의 역할을 확인해 채운다.
- 새 파일이 필요한 경우 기존 책임과 겹치는지 확인하고 자기 소유 경로에 추가한다. 공통 안내·상세 설계 변경은 민규가 반영한다. 종현은 필요한 문서·공통 파일·마이그레이션 변경을 자신의 요청 폴더에 남긴다.
- 2026-09-23 문서 동기화 시점의 신규 TypeScript 파일은 주석과 `export {};`만 있는 미구현 골격이다. `config.toml`은 미구성 주석, `deno.json`은 빈 객체다. 이후 작업에서는 실제 상태를 다시 확인한다. 파일 존재·구문 검사만으로 기능·환경·배포 준비가 완료됐다고 설명하지 않는다.
- 구현 완료 시 실제 기능에 맞춰 TODO·상태와 관련 계약을 갱신하고 의미 있는 검증 결과를 기록한다. 미정 정책·공급사를 임의 확정하거나 가짜 성공으로 대체하지 않는다.

## 종현 인수인계: 계획부터 설명

사용자가 “나 종현이야”, “종현입니다”처럼 자신을 종현이라고 밝히면 다음 인수인계 절차를 따른다. 이 자기소개는 작업 담당 구분이며 시스템 접근 권한을 부여하는 인증이 아니다.

1. `PLAN.md`와 `PLAN_상세설계.md`를 읽고, 상세 설계 11장의 담당 분류와 현재 저장소 상태를 확인한다. 확정 정책·기술 제안·이미 구현된 내용과 검증 결과를 구분한다.
2. 첫 응답은 한국어로 **담당 범위와 우선 작업 계획**을 명확히 설명한다. 계획을 준비하기 위한 읽기·상태 확인은 가능하지만, 자기소개만으로 코드·DB·환경 변경을 시작하지 않는다.
3. 설명에는 아래 내용을 포함한다.
   - 문서 안내: 첫 응답에 `PLAN.md`·`PLAN_상세설계.md`·`AGENTS.md`의 클릭 가능한 링크를 포함한다. 링크는 현재 작업 공간의 실제 경로를 사용하고 민규 컴퓨터의 절대 경로를 다른 환경에 그대로 사용하지 않는다.
   - 상세 설계 문서의 역할과 읽을 부분: 백엔드·AI 구조와 협업 기준을 담고 있으며, 11장 담당 업무·연결 계약을 먼저 읽고 6~8장 AI·작업 흐름, 9장 검증 기준을 확인하도록 안내한다.
   - 프로젝트 지침의 역할: 최신 기준과 추가 사용자 결정, 계획 우선 설명, 담당 경계와 기존 작업 보존, 복잡한 작업의 에이전트 팀 활용 기준을 담고 있다고 요약한다. 최신 계획의 서비스 정책을 상세 설계·지침과 연결해 설명한다.
   - 종현담당: AI 탐색·리뷰 요약·공개 검색·장소 및 행사 연동·작업 실행·AI 평가.
   - 민규담당과의 경계: 회원 인증·핵심 서비스·DB 권한·RPC·마이그레이션·공통 환경은 민규가 주담당이다.
   - 가장 먼저 할 일: 공개 검색, 공개 후기·revision, 요약 게시, 작업 처리, 완료·공개 시점, 행사 저장·조회 모드의 입출력·오류·권한 계약을 정리한다. 상세 설계 11.3장의 검색·반환 권한 분리, 수동 완료의 양쪽 확인, 후기·대화 정책을 반영한다.
   - 다음 순서: 가상 데이터·어댑터 준비 → 공개 검색·장소·행사 처리 → AI 탐색 → 리뷰 요약·작업 처리 → 실제 로컬 연결·통합 검증. 현재 진행 상태에 맞춰 완료된 단계는 건너뛴다.
   - 단계별 산출물, 완료 기준, 민규에게 필요한 연결 사항, 종현이 독립적으로 진행할 수 있는 일.
   - 동시 작업 안내: [협업 규칙](docs/collaboration/README.md)을 연결하고 종현 소유 파일만 편집하며 민규 파일은 요청 문서로 변경을 의뢰한다고 설명한다. 종현의 별도 복제본에서 `yumidang.actor=jonghyun`과 `.githooks` 설정이 필요한지 읽기 확인 후 안내한다. 자기소개만으로 공유 작업 공간의 작업자 설정을 바꾸지 않는다.
4. 첫 작업 단위를 구체적으로 제안한다. 예: “공개 검색과 리뷰 요약의 입출력 계약·가상 응답을 먼저 문서화하겠습니다.” 모델·공급사·예산·운영 정책이 미정이면 선택이 필요하다고 표시하고 임의 확정하지 않는다.
5. 자기소개만 받은 경우 계획 설명으로 응답을 마친다. 별도로 작업 시작을 요청받았거나 이미 해당 작업을 지시받았다면 그 범위에서 진행하며, 같은 승인을 반복해서 요구하지 않는다.

목표 구조가 문서에 있다는 이유만으로 코드가 구현되어 있다고 설명하지 않는다. 기존 HTML·DB 이력과 다른 담당자의 작업을 보존한다.
