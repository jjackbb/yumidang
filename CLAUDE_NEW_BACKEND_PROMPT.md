# Claude Code 실행 프롬프트 — yumidang6 신규 백엔드 구축

아래 작업을 `/Users/b/Documents/Antigravity/yumidang6`에서 수행해 주세요.

## 목표

현재 앱은 브라우저 저장소와 시연 데이터만 사용하는 React/Vite 프론트엔드 프로토타입입니다. 새로 만든 Supabase 계정의 **신규 프로젝트 하나만** 연결해, 스키마·인증·권한·데이터 저장·실시간 동기화를 처음부터 다시 설계하고 구현해 주세요.

`yumidang-rebuild` 폴더와 그 프로젝트는 별도 작업물이므로 조회·연결·수정·삭제하지 마세요. 이전 백엔드 파일을 Git 과거 커밋에서 복원하거나 옛 스키마를 복사하지 마세요.

## 1. 시작 전 확인

1. `pwd`, `git status`, `git remote -v`로 작업 위치와 변경 상태를 확인하세요.
2. 현재 삭제/수정된 파일은 의도된 정리 작업입니다. `git reset`, `git checkout -- .`, `git restore .`, 강제 삭제로 되돌리지 마세요.
3. `README.md`, `src/types.ts`, `src/App.tsx`, `src/data/`, 주요 화면 컴포넌트를 읽고 현재 사용자 흐름과 로컬 상태 계약을 먼저 정리하세요.
4. 새 계정 외의 기존 조직·프로젝트에는 어떤 쓰기 작업도 하지 마세요.

## 2. 새 Supabase 계정과 프로젝트 연결

1. 설치된 CLI가 없으면 `npx supabase --help`부터 확인하고, 실제 CLI 버전의 도움말에 맞는 명령만 사용하세요.
2. `npx supabase login`으로 **새 계정**에 로그인하세요. 브라우저 인증이나 access token 입력이 필요하면 사용자에게 그 단계만 요청하고, 토큰을 채팅·파일·Git 기록에 남기지 마세요.
3. 로그인 후 프로젝트 목록을 조회해 새 계정의 조직명·프로젝트명·project ref를 사용자에게 보여 주세요.
4. 프로젝트가 아직 없다면 이름·리전·조직을 사용자와 확인한 뒤 새 계정에 생성하세요. 기존 프로젝트를 임의로 선택하지 마세요.
5. 선택한 신규 project ref를 한 번 더 표시하고 확인한 뒤 이 저장소에 link 하세요.
6. 프론트에는 public URL과 publishable key만 사용하세요. service role, secret key, DB 비밀번호, SMS 공급자 비밀값은 브라우저 코드나 Git에 넣지 마세요.
7. `.env.local`은 Git에서 제외하고, `.env.example`에는 값이 없는 변수명과 설명만 추가하세요.

## 3. 구현 원칙

한꺼번에 전체 스키마를 만들지 말고 아래 수직 기능 단위로 진행하세요. 각 단계는 **화면 상태 → 데이터/API 계약 → 마이그레이션 → RLS → 프론트 연결 → 실패 UI → 테스트**까지 끝낸 뒤 다음 단계로 넘어가세요.

1. 로그인과 사용자 프로필
2. 동행 공고 생성·조회·수정·마감
3. 참여 신청·취소·거절·수락과 1:1 매칭 확정
4. 약속·채팅·알림과 실시간 수신
5. 동행 완료·상호 평가·당도 변경 이력
6. 차단·신고·운영 권한

현재 `src/types.ts`의 타입은 요구사항 참고 자료일 뿐 DB 스키마 정답이 아닙니다. 특히 신청 수락은 동시에 두 번 확정되지 않도록 서버 트랜잭션/RPC와 DB 제약으로 보호하고, 상태 변경 이력과 권한 검사를 함께 설계하세요.

모든 노출 테이블에 RLS를 켜고 다음 권한을 SQL 테스트로 검증하세요.

- 비회원이 볼 수 있는 공개 공고와 공개 프로필 범위
- 본인 프로필의 공개/비공개 필드 분리
- 작성자만 공고를 수정·마감할 수 있음
- 신청자는 자신의 신청, 호스트는 자기 공고에 온 신청만 볼 수 있음
- 확정된 두 사용자만 해당 채팅방·상세 장소·약속을 볼 수 있음
- 차단 관계에서는 상대 콘텐츠와 초대/메시지 접근이 제한됨
- 관리자 권한을 프론트 값으로 위조할 수 없음

SQL 변경은 재현 가능한 migration 파일로만 관리하세요. Dashboard에서 수동으로 바꾼 설정이 있으면 같은 결과를 만드는 migration 또는 문서화된 설정 기록을 남기세요. seed 데이터는 가짜 사용자/공고임을 명확히 표시하고 운영 데이터와 분리하세요.

## 4. 인증 조건

제품 방향은 휴대폰 문자 인증입니다. 새 프로젝트에서 Phone Auth가 실제로 활성화되었는지, 어떤 SMS 공급자와 비용/발송 제한이 필요한지 먼저 확인하세요. 공급자 설정이 없으면 실제 SMS가 된다고 주장하거나 테스트 인증을 실제 본인 인증으로 표시하지 마세요. 그 경우 프론트의 로컬 체험 로그인은 유지하고, 실 SMS 연결이 막힌 이유와 필요한 사용자 작업을 기록하세요.

## 5. 프론트 전환 방식

- 각 수직 기능이 완성될 때까지 현재 브라우저 저장소 흐름을 깨지 마세요.
- 저장소 계층 또는 기능별 adapter를 두어 로컬 구현과 신규 서버 구현의 경계를 명확히 하세요.
- 네트워크 loading/empty/error/retry 상태를 화면에 구현하세요.
- 낙관적 UI를 사용하더라도 서버 실패 시 원래 상태로 복구하고 사용자에게 알려 주세요.
- 채팅과 신청 상태는 서로 다른 두 계정/브라우저에서 새로고침 없이 전달되는지 확인하세요.
- 프론트에서 직접 허용하면 안 되는 다중 행 상태 변경은 RPC/Edge Function 등 서버 경계에서 처리하세요.

## 6. 검증과 기록

각 단계에서 `npm run lint`, 관련 단위 테스트, `npm run build`를 실행하세요. DB에는 최소한 다음 검증을 포함하세요.

- 익명/사용자 A/사용자 B/작성자/비작성자별 RLS 허용·거부
- 동일 신청 동시 수락 시 하나만 성공
- 재시도 시 중복 신청·중복 메시지·중복 평가가 생기지 않음
- 실시간 구독 실패 후 재연결과 중복 이벤트 처리
- 로그아웃 후 개인 데이터가 화면과 캐시에서 노출되지 않음

작업 기록에는 아래를 남기세요.

- 새 조직명·프로젝트명·project ref(비밀값 제외)
- 추가한 migration 목록과 적용 결과
- RLS 정책 표
- 프론트에서 로컬 구현을 서버 구현으로 바꾼 기능 목록
- 실제 실행한 테스트와 결과
- `NOT_RUN`, `BLOCKED`, 수동 설정이 필요한 항목

Vercel 환경변수 변경, 배포, Git push는 먼저 로컬 구현과 검증을 끝낸 뒤 사용자에게 최종 변경 내용을 보여 주고 별도 승인을 받아 진행하세요.

먼저 위 범위를 기준으로 **1단계 로그인/프로필의 데이터 계약과 마이그레이션 초안**, 그리고 새 계정 연결 상태를 보고한 뒤 구현을 계속하세요. 중요한 정책이 비어 있을 때만 1~3개의 핵심 질문을 하고, 나머지 확인·설계·로컬 작업은 계속 진행하세요.
