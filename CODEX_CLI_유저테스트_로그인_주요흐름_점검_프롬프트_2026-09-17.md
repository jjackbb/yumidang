# Codex CLI 작업 프롬프트 — 유저테스트 로그인 및 주요 흐름 점검

아래 내용을 `/Users/b/Documents/Antigravity/yumidang`에서 실행 중인 Codex CLI에 그대로 전달하세요.

---

유저테스트 직전 스모크 테스트를 실행해 주세요. 실제 Supabase 원격 프로젝트와 브라우저를 사용해 `010-5555-5555` 계정의 로그인 및 현재 한 계정으로 확인 가능한 주요 흐름을 검증하고, 결과를 증거와 함께 보고하세요. 계획만 작성하지 말고 실행까지 완료하세요.

## 1. 대상과 절대 조건

- 저장소: `/Users/b/Documents/Antigravity/yumidang`
- Supabase 대상: **`bndguguarijmghnkenvt` (`yumidang`)**
- 브라우저 우선 대상: **`https://yumidang6.vercel.app`**
- 로컬 확인이 필요하면 현재 소스로 Vite를 실행하고 원격 Supabase에 연결하세요.
- 테스트 계정: `010-5555-5555`
- 테스트 인증은 화면에 안내된 고정 테스트 OTP를 사용하며 **실제 SMS를 발송하지 마세요.**
- 기존 Auth 사용자 1명, 프로필 1개, 공고 6개를 유지하세요.
- 계정·프로필·공고·비공개 장소·신청·채팅·동행·평가·Storage 객체를 생성·수정·삭제하지 마세요.
  - 로그인 과정에서 Supabase가 세션과 `last_sign_in_at` 같은 Auth 메타데이터를 갱신하는 것은 허용합니다.
- SQL `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `DROP`, 원격 `db reset`을 실행하지 마세요.
- UI나 제품 코드, 마이그레이션, 환경 변수, Edge Function 설정을 변경하지 마세요.
- 커밋·푸시·배포하지 마세요.
- 실패를 고치기 위해 데이터를 임의 수정하지 마세요. 실패를 재현하고 원인과 가장 작은 후속 조치를 보고하세요.
- 브라우저·터미널·보고서에 Access token, Refresh token, Secret key, 전체 전화번호, 생년월일, 정확한 장소를 출력하거나 저장하지 마세요.
- 개인정보가 보이는 스크린샷은 저장하지 마세요. 텍스트 기반의 마스킹된 증거만 남기세요.

## 2. 먼저 읽을 자료

현재 사용자 지시를 가장 우선하고 다음 파일을 확인하세요.

- `AGENTS.md`가 있으면 해당 지침
- `HANDOFF.md`
- `docs/backend-implementation/RUNBOOK.md`
- `docs/backend-implementation/STATE.md`
- `docs/backend-implementation/RETEST-2026-09-17.md`
- `src/components/AuthModal.tsx`
- `src/auth/testPhone.ts`
- `supabase/functions/test-phone-auth/handler.ts`

테스트 전에 `/mcp`로 연결된 프로젝트가 `bndguguarijmghnkenvt`인지 확인하세요. 다른 프로젝트라면 테스트를 시작하지 말고 `BLOCKED`로 보고하세요.

## 3. 데이터 비변경 사전 확인

읽기 전용으로 다음 기준값을 확인하고 개인 정보는 마스킹하세요.

- Auth 사용자 수: 예상 1
- 프로필 수: 예상 1
- 지정 계정이 작성한 공고 수: 예상 6
- 공고 6개의 상태, 모집 마감, 시작 시각이 현재 기준으로 유효한지
- 테스트 인증 Edge Function이 활성 상태인지
- `010-5555-5555` 사용자의 Auth `app_metadata.test_phone_auth`가 `true`인지

특히 현재 `test-phone-auth` 구현은 임의 테스트 번호 로그인 시 `app_metadata.test_phone_auth = true`를 요구합니다. 이 값이 없으면 로그인은 실패하는 것이 정상입니다. 값을 직접 수정하지 말고 사전조건 실패로 기록하세요.

원격 데이터 조회 권한이 부족하면 브라우저에서 관찰 가능한 항목만 검증하고 나머지는 `NOT_RUN`으로 표시하세요.

## 4. 데이터에 영향을 주지 않는 코드 검사

다음을 실행하세요.

```bash
npm run lint
npm test
npm run build
npm run harness:preflight
```

`harness:preflight`는 계정을 만들지 않는 요청 단계만 사용하는지 현재 코드를 확인한 후 실행하세요. 데이터 변경 가능성이 발견되면 실행하지 말고 `NOT_RUN`으로 기록하세요.

다음 명령은 계정과 공고를 추가하므로 **실행 금지**입니다.

```bash
npm run test:harness
npm run test:harness:api
npm run harness:01
npm run harness:02
npm run harness:03
npm run harness:04
npm run harness:05
npm run harness:06
npm run harness:07
npm run harness:08
npm run harness:09
npm run harness:full-cycle
```

## 5. 실제 브라우저 테스트

새 브라우저 컨텍스트를 사용해 저장된 로그인 상태 없이 시작하세요. Chromium 모바일 뷰포트 약 `390 × 844`를 우선 사용하고, 화면이 깨지는 경우 데스크톱에서도 재현 여부를 확인하세요.

Playwright가 필요하면 `docs/backend-implementation/RUNBOOK.md`에 기록된 기존 모듈과 Chromium 경로를 사용하세요. 새 패키지는 설치하지 마세요.

각 시나리오는 다음을 기록합니다.

- 입력 전제
- 수행 행동
- 기대 결과
- 실제 관찰
- `PASS`, `FAIL`, `NOT_RUN`, `BLOCKED`
- 실패 시 재현 단계와 콘솔/네트워크 오류의 민감정보 제거 요약

### A. 비로그인 진입

1. `https://yumidang6.vercel.app`을 새 브라우저 컨텍스트에서 엽니다.
2. 앱이 흰 화면이나 무한 로딩 없이 표시되는지 확인합니다.
3. 브라우저 콘솔의 처리되지 않은 오류와 실패한 주요 네트워크 요청을 확인합니다.
4. 공개 모집 목록에 아래 유저테스트 공고 6개가 보이는지 확인합니다.
   - 성수역 팝업스토어 같이 구경해요
   - 홍대 돈까스 저녁 식사 동행 구해요
   - 서울숲에서 가볍게 산책해요
   - 대학로에서 연극 한 편 같이 봐요
   - 용산에서 영화 보고 이야기 나눠요
   - 망원시장 먹거리 구경 같이 해요
5. 숨김 처리된 기존 공고가 공개 모집 목록에 나타나지 않는지 확인합니다.
6. 공고 상세를 열어 공개 지역·일정·설명·카테고리가 표시되는지 확인합니다.
7. 비로그인 상태에서 정확한 만남 장소, 전체 실명, 전화번호, 생년월일이 노출되지 않는지 확인합니다.

### B. 검색과 탐색

데이터를 변경하지 않는 범위에서 확인합니다.

1. `성수` 검색 시 성수 팝업 또는 서울숲 공고가 결과에 나타나는지
2. `식사` 카테고리에서 홍대 돈까스와 망원시장 공고가 나타나는지
3. 검색/필터를 해제하면 전체 6개 공고로 돌아오는지
4. 공고 상세를 닫았을 때 이전 목록 상태로 자연스럽게 복귀하는지

UI에 해당 검색 또는 필터가 없다면 실패로 만들지 말고 `NOT_RUN`과 실제 UI 구조를 기록하세요.

### C. 실제 테스트 로그인

1. 로그인 화면을 엽니다.
2. 로그인 모드인지 확인합니다. 회원가입 모드로 새 계정을 생성하지 마세요.
3. `010-5555-5555`를 입력합니다.
4. 인증번호 요청을 실행합니다.
5. 화면에서 실제 문자를 보내지 않는 테스트 인증임을 안내하는지 확인합니다.
6. 화면에 제공된 테스트 OTP로 인증합니다.
7. 실제 Supabase 세션이 생성되고 로그인 UI가 닫히는지 확인합니다.
8. 기존 프로필이 조회되어 추가 회원가입이나 기본 프로필 입력 단계로 이동하지 않는지 확인합니다.
9. 로그인 후 새로고침해도 세션과 사용자 상태가 유지되는지 확인합니다.

인증 요청이나 검증이 실패하면 다음을 구분하세요.

- Edge Function 비활성 또는 만료
- 배포 환경의 `VITE_TEST_PHONE_AUTH` 누락
- 계정의 `app_metadata.test_phone_auth` 누락
- 전화번호 정규화 차이
- 프로필 누락
- 배포본과 현재 소스의 차이

실패해도 Auth 사용자나 메타데이터를 직접 수정하지 마세요.

### D. 로그인 후 주요 흐름

1. Me 또는 프로필 화면에 본인 정보가 표시되는지 확인합니다.
2. 전체 실명·생년월일·전화번호가 공개 목록이나 타인용 프로필 카드에 섞여 나오지 않는지 확인합니다.
3. 내 공고 화면에서 유저테스트 공고 6개가 중복 없이 보이는지 확인합니다.
4. 각 공고의 제목, 상태, 일정이 공개 목록과 일치하는지 확인합니다.
5. 작성자로 공고 상세를 열었을 때만 정확한 만남 장소를 볼 수 있는지 확인합니다. 정확한 장소 원문은 증거에 기록하지 마세요.
6. 알림·채팅·동행 화면이 데이터가 없는 상태에서도 오류 없이 빈 상태를 표시하는지 확인합니다.
7. 공고 작성, 수정, 삭제, 신청, 채팅 전송, 매칭, 완료, 평가 버튼은 **누르지 마세요.** 데이터 변경 없이 화면 진입과 상태만 확인하세요.
8. 모바일 화면에서 하단 내비게이션, 모달 닫기, 뒤로 가기와 스크롤이 막히지 않는지 확인합니다.
9. 처리되지 않은 콘솔 오류와 주요 네트워크 실패가 없는지 확인합니다.

### E. 로그아웃과 재로그인

1. 로그아웃을 실행합니다.
2. 새로고침 후 로그인 전 상태인지 확인합니다.
3. 동일 계정으로 다시 로그인합니다.
4. 같은 프로필과 같은 공고 6개가 유지되는지 확인합니다.
5. 테스트 전후 Auth 사용자·프로필·공고 개수가 각각 `1 / 1 / 6`으로 유지되는지 읽기 전용으로 확인합니다.

## 6. 운영 URL과 로컬 결과 구분

운영 URL에서 실패했지만 현재 로컬 소스에서는 통과할 가능성이 있으면, 동일 브라우저 시나리오 중 실패한 최소 범위만 로컬 Vite에서 다시 실행하세요.

- 운영과 로컬을 각각 `PRODUCTION`과 `LOCAL`로 구분
- 운영 실패·로컬 통과이면 배포본 차이 가능성으로 기록
- 둘 다 실패이면 구현 또는 원격 설정 문제로 기록
- 운영만 검사하고 로컬을 실행하지 않았다면 로컬은 `NOT_RUN`
- 로컬 통과를 운영 통과로 표현하지 않기

## 7. 이번 테스트로 확인할 수 없는 범위

현재 Auth 계정이 1개뿐이므로 다음은 실제 브라우저 종단 테스트가 불가능합니다.

- 다른 사용자의 공개 프로필 조회
- 공고 신청 및 신청 철회·재신청
- 작성자의 신청 수락·거절
- 1:1 채팅과 Realtime 양방향 수신
- 최종 매칭
- 동행 완료·이의 제기
- 블라인드 상호 평가와 평가 공개

이를 실패로 기록하지 말고 **두 번째 계정 부재로 `NOT_RUN`**이라고 명시하세요. 과거 하네스 PASS는 현재 한 계정 상태의 실측 결과로 재사용하지 마세요.

## 8. 증거와 결과 보고

다음 파일을 새로 작성하세요.

`docs/backend-implementation/USER-TEST-SMOKE-2026-09-17.md`

보고서에는 다음을 포함하세요.

- 실행 시각과 대상 URL
- 대상 Git commit과 작업 트리 상태
- 대상 Supabase project ref
- 코드 검사 결과
- A~E 각 시나리오의 기대 결과, 실제 관찰, 판정
- 테스트 전후 안전한 개수: Auth 사용자·프로필·공고
- 콘솔/네트워크 오류 요약
- 실제 SMS가 발송되지 않았음을 관찰한 근거
- 개인정보·토큰·정확한 장소를 증거에 저장하지 않았다는 확인
- `NOT_RUN` 범위와 이유
- 유저테스트 진행 가능 여부를 다음 중 하나로 판정
  - `GO`: 로그인과 데이터 비변경 주요 흐름이 모두 통과
  - `CONDITIONAL GO`: 핵심 로그인과 공고 흐름은 통과했지만 비핵심 결함이 있음
  - `NO-GO`: 로그인 실패, 공고 6개 미표시, 개인정보 공개 노출, 치명적 화면 오류 중 하나 이상
- 실패가 있다면 우선순위, 재현 단계, 가장 작은 후속 조치

기존 보고서나 증거 파일을 덮어쓰지 마세요. 같은 경로가 이미 있으면 시각을 붙인 새 파일을 만드세요.

완료 답변에는 다음만 간결하게 알려 주세요.

1. `GO` / `CONDITIONAL GO` / `NO-GO`
2. 실제 로그인 성공 여부
3. 공고 6개 및 테스트 전후 데이터 개수
4. 가장 중요한 실패 또는 제한
5. 보고서 경로

테스트가 끝났다고 사용자 가치나 유저테스트 성공이 증명된 것으로 표현하지 마세요. 이번 작업은 유저테스트 직전의 기술적 준비 상태 확인입니다.

---

