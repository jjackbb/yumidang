# 유미당 — HTML 유지·기존 Supabase 기반 백엔드 설계 계획

## 1. 기준과 작업 범위

- 작성일: 2026-09-23.
- 서비스 정책·화면·사용자 흐름의 기준: [IA.md](docs/planning/design/IA.md), [USER_FLOW.md](docs/planning/design/USER_FLOW.md).
- 기존 구조 설계의 출처: [PLAN_원본.md](PLAN_원본.md). 충돌하는 서비스 기준은 최신 IA·USER_FLOW에 맞추고, 충돌하지 않는 기술 방향은 유지한다.
- 이 문서는 폴더·데이터 경계·API 역할·AI 처리·검증 환경에 대한 계획이다. 화면별 세부 배치는 IA, 행동·예외·복귀는 USER_FLOW를 참조한다.
- 최신 문서에 미정으로 남아 있던 AI 요약 개수·대화 보관 정책은 2026-09-23 사용자 답변으로 확정했다. 한마디 있는 공개 후기 3개부터 요약하고, AI 대화는 현재 탐색 중만 유지하며 서비스 서버에 대화 이력을 저장하지 않는다. 이 두 항목은 6장의 최신 결정을 우선한다.

기존 기술 방향은 다음과 같이 유지한다.

- 기존 Supabase와 DB 변경 이력을 유지하고 필요한 변경만 후속 migration으로 추가한다. DB를 초기화하거나 기존 이력을 덮어쓰지 않는다.
- 검증 환경은 기존 원격 DB와 분리한 로컬 Supabase로 구성한다. 실제 사용자 데이터를 복사하거나 이전 프로젝트 백업을 만들지 않는다.
- 기존 React 화면·프론트 연결 코드·테스트·실행 설정은 재사용하지 않는다. React 전환이나 화면 전체 재작성은 계획에 포함하지 않는다.
- 이번에는 계획 문서만 작성한다. 현재 HTML을 유지하되, 후속 구현에서는 최신 가입·검색 흐름과 다른 부분을 수정하고 백엔드에 연결해야 한다. ‘HTML 유지’를 기존 예시 동작을 영구히 고정한다는 뜻으로 쓰지 않는다.
- HTML 예시 전환, 로컬 검증, 실제 외부 연동, 원격 DB 검증, 배포 결과를 각각 구분한다.

## 2. 목표 폴더와 역할

다음은 기존 계획에서 이어받은 목표 구조다. 현재 폴더가 이 구조로 정리됐거나 기능이 구현됐다는 뜻은 아니다.

```text
backend/
├── README.md
├── contracts/                     # 화면별 요청·응답·오류·권한 규칙
└── supabase/
    ├── config.toml                # 새 로컬 실행 설정
    ├── migrations/                # 기존 이력 유지, 후속 변경 추가
    ├── seeds/                     # 개인정보 없는 가상 테스트 데이터
    └── functions/
        ├── signup/                # PASS 결과·가입 자격·중복 확인·계정 연결
        ├── verification/          # 선택 이메일·1원 계좌 인증
        ├── places/                # 장소 검색·주소 입력 연동
        ├── event-sync/            # 공연·전시·축제 수집
        ├── ai-chat/               # 자연어 공고·행사 탐색
        ├── review-summary-worker/ # AI 요약 브리핑 생성
        ├── scheduled-jobs/        # 완료·공개 시점·갱신·재시도
        └── _shared/
            ├── contracts/         # 실제 요청·응답 형식
            ├── auth/              # 로그인·세션·회원 자격·접근 권한
            ├── services/          # 기능별 처리와 DB 호출
            ├── integrations/      # 외부 업체별 연결 코드
            ├── ai/
            │   ├── chatbot/
            │   ├── review-summary/
            │   └── providers/
            ├── jobs/              # 작업 등록·중복 방지·재시도
            └── observability/     # 오류·처리 시간·사용량 기록

tests/                             # 기존 테스트 코드 재사용 안 함
├── database/                      # DB 제약·접근 권한
├── functions/                     # 서버 함수
├── contracts/                     # 화면에 필요한 응답 형식
├── integration/                   # 여러 사용자 간 실제 처리
├── ai/                            # 검색·요약 평가
└── fixtures/                      # 개인정보 없는 검증 자료

tools/
└── local/                         # 실행·검증 명령과 환경 점검
```

| 영역 | 책임 |
|---|---|
| DB | 관계·상태별 접근 권한, 양쪽 동의, 중복 신청·평가 방지, 칭찬 집계 |
| 서버 함수 | 비밀키가 필요한 외부 인증·장소·행사·AI 연결, 인증 결과 검증 |
| AI | 자연어 조건 해석·공개 원문 요약. 가입 자격·매칭 확정·지급 여부 판단 제외 |
| 예약 작업 | 행사 갱신, 자동 완료, 리뷰 공개 보류·기한 처리, 요약 생성·재시도 |

같은 규칙을 화면·서버·AI에 복사하지 않고 서버와 DB의 공통 처리 경로를 사용한다. 브라우저의 역할·인증 완료 표시만 믿지 않고 실제 사용자와 대상의 관계를 확인한다. 비밀키는 서버에서 관리하고, DB의 접근 정책과 API 응답 필드 모두에서 공개 범위를 지킨다.

## 3. 데이터·API 연결 기준

### 3-1. 가입·로그인·프로필

신규 가입은 PASS 한 번으로 성명·성별·생년월일·휴대폰번호·DI를 확인한다. 성인 여성 자격을 검증하고 DI로 중복 가입을 막는다. PASS 미연동 상태에서 이름·생년월일 수기 입력으로 가입을 대신하는 경로는 현재 서비스 설계에 포함하지 않는다.

가입 흐름은 PASS 결과 검증 → 자격·중복 확인 → 계정·세션 연결 → S06 사진 등록·선택 성향 입력 → 가입 완료 버튼 → 안내 팝업 → S00 홈이다. 사진 선택만으로 가입이 완료되지는 않는다. 취향·대화 방식·MBTI는 비워도 가입할 수 있다.

기존 회원의 일반 로그인은 등록된 휴대폰번호에 보낸 문자 일회용 인증번호를 사용한다. 로그인 후 원래 목적 화면으로 복귀하고 목적이 없으면 홈으로 간다. 휴대폰번호 변경·로그인 불가 계정복구·탈퇴 후 재가입은 PASS 재인증 후 해당 목적 흐름으로 복귀한다.

- PASS 결과는 서버에서 확인하며 사용자가 성명·성별·생년월일을 직접 입력·수정해 자격 확인을 우회할 수 없다.
- 자격 불충족 시 계정을 만들지 않고 이용 조건을 안내한다. 실패 화면에 상세 성별·생년월일 값은 노출하지 않는다.
- PASS 외 신분증 촬영·제출이나 같은 신원정보 재입력 단계를 추가하지 않는다.
- PASS와 Supabase 계정·세션을 연결하는 방식은 연동사 선정 후 기술 검증한다. 가입 시 두 번째 인증을 요구하지 않는 것을 완료 조건으로 둔다.
- 기존 계정·수기 정보·로컬 테스트 데이터를 실제 PASS 인증 완료로 자동 승격하지 않는다.
- 사진 교체 실패 시 기존 사진을 유지하고 마지막 사진 삭제는 허용하지 않는다. 성향 편집 실패 시 입력을 유지한다.

### 3-2. 선택 이메일·계좌 인증과 직접 지급

학교·직장 이메일은 가입 후 S21 → S07에서 선택 인증한다. 이메일·도메인은 다른 회원에게 공개하지 않는다. 배지는 해당 이메일의 사용 가능 여부를 뜻하며 재학·재직·안전을 보증하지 않는다.

| 동행 유형 | 지급 방향 | 계좌 인증 조건 |
|---|---|---|
| 무료 | 동행 대금 없음 | 필수 아님 |
| 유료 요청형 | 작성자 → 신청자 | 작성자·신청자 모두 필요 |
| 유료 제공형 | 신청자 → 작성자 | 작성자·신청자 모두 필요 |

유료 작성자가 최종 ‘등록하기’를 누를 때, 유료 신청자가 최종 ‘신청하기’를 누를 때 서버에서 계좌 인증 상태를 확인한다. 미인증이면 해당 요청을 차단하고 S22로 안내한다. 입력을 보존해 인증 후 S04 또는 S08로 돌아오며, 사용자가 다시 제출해야 한다. 인증 성공을 자동 등록·신청·매칭으로 처리하지 않는다.

1원 계좌 인증은 PASS 성명과 제공사가 확인한 예금주가 공급사의 명의 대조 기준에 따라 일치해야 한다. 본인 개인 명의 계좌만 허용하며 개인사업자·법인 계좌는 초기 출시에서 지원하지 않는다. 사용자가 예금주명을 입력·수정해 우회할 수 없다.

계좌 배지는 이메일 배지와 구분해 공개 프로필·공고 카드·공고 상세에 표시한다. 변경·취소·만료·무효 처리 시 숨긴다. 클릭 시 계좌 확인이 안전·대금 지급을 보증하지 않는다고 안내한다. 인증에 사용한 은행명·계좌번호·예금주명은 다른 사용자에게 자동 공개하지 않는다. 검토용 인증 코드는 서비스 화면에 표시하지 않는다.

현재 최종 동의 흐름에는 S11에서 S22로 돌아가는 추가 계좌 인증 단계를 넣지 않는다. 등록·신청 후 인증 효력이 달라진 경우의 확정 허용 여부는 최신 문서에 정해져 있지 않으므로 별도 미정 항목으로 둔다. 서버의 유효성 조회와 사용자에게 재인증을 요구하는 흐름을 같은 의미로 취급하지 않는다.

대금은 확정된 두 사용자가 1:1 채팅에서 방법·시점을 합의해 직접 지급한다. 유미당은 결제대행·대금 보관·에스크로·자동 송금·정산·자동 환불·지급 완료 보증을 제공하지 않는다. 채팅에서 전달한 계좌를 인증 계좌라고 자동 보증하지 않는다.

### 3-3. 공고·장소·검색·정보 공개

공고 작성은 카테고리·제목·소개·무료/유료 유형·금액·시작/종료 날짜와 시간을 받는다. 종료는 시작보다 늦어야 하며 날짜를 넘기는 일정도 허용한다. 장소 지정은 카카오 장소명 검색·선택 후 주소 자동 입력 또는 우편번호 검색·주소 선택의 두 경로다. 상세 만남 지점은 별도로 입력한다.

행사를 연결해도 작성한 일정을 자동 변경하지 않는다. 선택 모드에서 돌아올 때 입력을 유지한다. 등록 성공 시 생성한 공고 상세로 이동하고 실패 시 입력을 유지한다. 공고 수정·마감·삭제의 허용 시점과 관련 신청·대화 처리 정책은 미정으로 둔다.

일반 탐색과 AI의 공고 키워드 검색 대상은 제목·등록 장소명·등록 주소다. 공고 소개·리뷰까지 검색 범위를 확장하지 않는다. 카테고리·비용·일정·작성자 나이 필터는 유지한다. 장소 주변 찾기·좌표·반경·거리순 정렬은 추후 기능이다.

| 대상·관계 | 이름·위치 공개 기준 |
|---|---|
| 비로그인 | 시스템 별칭과 공개 지역. 신청·프로필 상세·채팅·나·AI는 로그인 안내 |
| 일반 회원·최종 동의 대기 | 마스킹 이름·공개 지역. 상대의 정확한 주소·상세 지점 제외 |
| 공고 작성자 | 자신의 입력 정보 열람·관리. 상대의 정보를 단독 선택만으로 공개하지 않음 |
| 양쪽 확정 당사자 | 서로의 전체 실명·정확한 주소·상세 만남 지점 |
| 확정 공고의 제3자 | 확정·마감 상태 확인, 새 신청 불가. 당사자 비공개 정보 제외 |

주소 검색에 쓰는 데이터와 검색 결과·상세 화면에 표시할 데이터는 구분한다. 주소가 검색어와 일치해도 확정 전 정확한 주소 표시 제한은 유지한다. 검색 일치만으로 장소를 추정할 가능성은 있으므로 기존과 같은 위치 비공개 효과를 보장한다고 표현하지 않는다.

비공개 상세 만남 지점은 검색 대상·검색 응답·AI 입력에서 제외한다. AI에 전달하는 검색 결과도 해당 사용자의 공개 범위를 지키도록 설계한다. 사용자가 대화에 직접 쓴 검색 주소와 DB에서 조회한 비공개 주소를 구분한다.

기존 `exact_location`에는 주소와 상세 안내가 섞여 있을 수 있으므로 자동으로 공개 필드에 옮기지 않는다. 구분된 새 정보가 없는 기존 공고는 기존 공개 지역까지만 표시한다. PASS 생년월일·휴대폰번호·DI는 다른 회원에게 공개하지 않는다.

### 3-4. 신청·대화·양쪽 최종 동의

S08에서 조건·금액·지급 방향을 확인하고 필수 소개 메시지로 신청한다. 제출 실패는 입력 유지·재시도, 성공은 신청 완료 후 S11 대화로 이어진다. 신청 단계의 일정 겹침은 안내 후 계속할 수 있지만, 확정 일정 충돌은 확정 불가로 구분한다. 기존 약속을 자동 취소하지 않는다.

S02 상대 선택, S13 받은 신청, S11 작성자 버튼은 같은 최종 동의 절차를 사용한다.

1. 작성자가 상대를 선택해 최종 동의를 요청한다.
2. 신청자가 같은 일정·공개 지역·금액·지급 방향을 확인한다.
3. 동의하지 않으면 확정 전 대화로 돌아간다. 동의하면 서버에서 조건 일치·사용자 권한·확정 가능 여부를 확인한다.
4. 양쪽 확정 후 약속 생성과 실명·상세 장소 공개를 연결한다.

작성자 선택만으로 약속을 확정하거나 다른 신청을 종료하지 않는다. 조건이 바뀌면 이전 요청으로 확정하지 않고 새 조건에 다시 동의하도록 한다. 단독 확정 우회 경로를 막고 조건 버전·일정 충돌·동시 처리를 확인한다. 동시 확정 경쟁의 세부 처리와 미선정 신청 종료 방식은 후속 계약에서 정한다.

채팅 전송 실패는 재전송을 제공하고 종료된 대화는 읽기 전용으로 둔다. 확정 전 철회는 사유와 처리 결과를 제공한다. 확정 동행 취소의 허용 범위·제재·취소 후 정보 접근은 미정이다.

### 3-5. 완료·평가·알림

USER_FLOW 12장의 시간 기준을 예약 작업 설계에 반영한다.

- 예상 종료 시각 이후 양쪽 모두 완료 처리할 수 있다. 미처리 시 예상 종료 +24시간에 자동 완료하며 취소·불발·분쟁은 제외한다.
- 완료 알림을 앱에서 확인할 수 있는 시점부터 24시간 동안 평가 공개·당도·완료 횟수 반영을 보류한다. 실제 알림 클릭 시점으로 바꾸지 않는다. 보류 중 평가 작성은 가능하다.
- 평가 작성 기간은 완료 시각부터 7일이다. 양쪽 제출 시 보류 종료 후 공개하고 한쪽만 제출하면 7일 기한에 공개한다.
- 이의 검토 중 작성·기한·공개를 보류한다. 동행 인정 후 남은 기간을 재개하고 최소 24시간을 보장한다. 분쟁 판단·증거·종결 절차는 미정이다.

평가는 좋아요/보통이에요/별로예요와 필수 별점 1~5, 선택 한마디를 받는다. 좋아요에만 칭찬 키워드를 최대 3개 허용한다. 보통·별로로 바꾸면 이전 칭찬은 제출하지 않는다. 제출 전 변경·실패 재시도를 허용하고 제출 성공 후 수정·중복 제출을 막는다. 제출 완료와 공개 완료를 구분한다.

공개 평가의 칭찬을 사용자별로 누적 집계해 상위 5개와 실제 횟수를 제공한다. S13·S14는 칭찬 차트 → AI 요약 브리핑 → 원문 리뷰 순서다. 당도 산식은 미정이며 별점을 임의 환산하지 않는다.

알림은 최종 동의 요청·양쪽 확정·완료/평가 안내를 구분한다. 클릭 시 대상·관계·현재 상태를 확인하며, 알림 클릭 자체가 동의·확정·완료를 실행하지 않는다.

### 3-6. 행사 연동

공연은 KOPIS, 서울 전시·축제는 서울 열린데이터광장, 전국 축제는 TourAPI를 연결할 설계다. 홈·일반 목록의 ‘이번 주 신규’, 선택 기간과 겹치는 ‘진행 중 포함’, 공고 작성용 ‘진행 중·예정 행사 선택’을 구분한다.

행사 선택은 작성 입력을 유지하고 일반 목록의 신규 기준을 강제하지 않는다. 행사 입장료와 동행 비용은 별도 항목이다. 기간이 일치해도 실제 운영일·회차·잔여석을 보장하지 않으며 제공 안내·공식 페이지 확인을 안내한다. 공연·뮤지컬 Top 10은 검토 영역으로 남긴다.

## 4. AI 처리 계획

| 구분 | AI 챗봇 | AI 요약 브리핑 |
|---|---|---|
| 실행 계기 | 사용자의 자연어 탐색 요청 | 공개 텍스트 후기 추가·공개 상태 변경 |
| 입력 | 현재 대화, 탐색 조건, 허용된 프로필 성향 | 공개된 후기 한마디 |
| 처리 | 조건 해석 → 필요 시 되묻기 → 공고·행사 검색 → 설명 | 원문 확인 → 요약 → 근거·공개 상태 재확인 |
| 출력 | 해석한 조건, 실제 결과 카드, 추천 이유·확인할 점 | 요약문, 기준 후기 수, 갱신 시각 |
| 실패 시 | 재시도 또는 S01 일반 탐색 | 원문 후기·칭찬 차트 계속 제공 |
| 금지 | 임의 SQL, 신청·매칭·결제 실행, 권한 밖 정보 조회 | 비공개 후기 사용, 원문에 없는 평가 생성 |

현재 대화에서 명시한 조건을 프로필보다 우선한다. 상대 MBTI 미입력만으로 자동 제외하지 않는다. 실제 공고와 행사 결과를 구분하고 공고 없는 행사를 신청 가능한 동행처럼 표현하지 않는다.

‘이번 주말 전시’는 주말과 기간이 겹치는 장기·신규 전시를 모두 포함하고 금요일 종료 전시는 제외한다. 신규만 명시하면 좁힌다. ‘근처·주변’ 요청에는 현재 미지원임을 안내하고 검색할 장소명·주소를 확인한다.

AI 대화는 현재 탐색 중에만 유지하고 서비스 서버에 대화 이력을 저장하지 않는다. 검색·응답 처리를 위한 요청 전달과 대화 이력 저장을 구분하며, 오류·사용량 기록에 대화 원문을 이력으로 남기지 않는다. 외부 AI 업체의 로그 보관 조건은 제공사 선정 시 별도로 확인한다.

요약은 한마디가 있는 공개 후기 3개 이상부터 제공한다. 한마디가 없는 평가와 비공개 후기는 기준 개수에서 제외한다. 공개 평가 총수와 요약 대상 텍스트 후기 수를 구분해 기준 후기 수를 표시한다. 원문이 부족하면 없는 내용을 만들어 요약하지 않는다.

요약을 열람마다 생성하지 않고 작업으로 생성·저장하는 기존 기술 방향은 유지한다. 포함된 후기가 비공개로 바뀌면 해당 요약도 노출 중단 후 재처리한다. 공개 상태와 적용할 개수 기준을 생성·노출 시 확인한다.

신고 요약·운영자 보조 AI는 후속 후보로만 남긴다. 칭찬 집계·매칭·알림·행사 수집은 일반 프로그램으로 처리한다.

## 5. 검증 환경과 확인 기준

로컬 Supabase를 기존 migration과 새 가상 데이터로 구성한다. CLI·컨테이너 실행 환경은 후속 구현 시 확인한다. 외부 제공사의 테스트 결과와 실제 운영 연동 결과를 구분한다.

| 범위 | 확인할 결과 |
|---|---|
| 가입·로그인 | PASS 자격·DI 중복 검증, 수기 우회 불가, 사진 필수·성향 선택, 일반 로그인과 예외 PASS 재인증 분리 |
| 복귀 | 기존 로그인은 목적 화면, 신규 가입은 홈, 계좌 인증은 입력 보존 후 직접 제출 |
| 정보 공개 | 작성자·확정 상대·일반 회원·제3자·비로그인별 이름·주소·인증정보 접근 제한 |
| 공고·검색 | 제목·장소명·주소 검색과 표시 권한 구분, 주변 검색 제외, 장소/우편번호 입력·시작/종료 검증 |
| 유료 | 등록·신청 시 인증 확인, 본인 개인 계좌 명의 대조, 무료 인증 비강제, 계좌정보 자동 공개 금지·배지 무효 시 숨김 |
| 매칭·대화 | 신청과 확정 분리, 작성자 단독 확정 차단, 조건 변경·권한·일정 충돌·동시 처리, 전송 실패 재시도 |
| 완료·평가 | 자동 완료 제외 조건, 공개 보류·작성 기한·분쟁 보류, 성공 후 수정 불가, 칭찬 조건·중복 제출 방지 |
| 행사·AI 탐색 | 신규/진행 중/선택 모드 구분, 주말 기간 겹침, 프로필보다 대화 조건 우선, 실제 결과·권한 있는 정보만 제공 |
| AI 요약 | 한마디 있는 공개 후기 2개에서는 생성하지 않고 3개부터 제공. 한마디 없는 평가·비공개 후기 제외, 비공개 전환 시 기존 요약 노출 중단·재판정, 실패해도 원문 제공 |
| 대화 보관 | 현재 탐색 중만 유지, 서비스 서버에 대화 이력 저장 안 함. 오류·사용량 기록에도 원문 이력을 남기지 않는지 확인 |

위 표는 앞으로 수행할 검증 계획이다. 이번 문서 작성에서 API·DB·브라우저·시간 경과 테스트를 실행한 것으로 기록하지 않는다.

## 6. 사용자 결정과 미정 정책

### 이번 문서 작성에서 확인한 결정

| ID | 근거 차이 | 사용자 결정 | 상태 |
|---|---|---|---|
| Q1 | 원본은 텍스트 공개 후기 3개부터. IA 3-4·USER_FLOW 11장은 한마디 없는 평가의 산입 여부 미정 | 한마디 있는 공개 후기 3개부터 요약. 한마디 없는 평가는 기준 개수에서 제외 | 2026-09-23 사용자 확정 |
| Q2 | 원본은 현재 탐색 중만 유지·서버 이력 미저장. IA 6장은 보관 미정, USER_FLOW 10장은 장기 자동 저장 제외 | 현재 탐색 중만 유지하고 서비스 서버의 대화 이력 미저장 유지. 외부 AI 업체 로그 조건은 별도 확인 | 2026-09-23 사용자 확정 |

두 항목은 사용자 답변으로 확정했으며 이 문서의 AI 처리·검증 기준에 반영했다. IA 3-4·6장과 USER_FLOW 10·11·14장의 관련 미정 문구는 후속 동기화가 필요하다. 이번 생성 범위는 PLAN.md이므로 기준 문서 원문은 수정하지 않았다.

### 후속 상세 설계에서 정할 항목

- PASS·문자·이메일·계좌 인증 제공사, PASS 계정·세션 연결 방식, 기존 미검증 계정 전환 방식.
- 인증 코드 만료·재시도·갱신, 이메일 지원 기관, 계좌 인증 효력 변경 후 신청·확정 처리. 무효 배지를 숨기는 규칙 자체는 확정이다.
- 최종 동의 요청 만료·철회, 동시 확정 경쟁·미선정 신청 처리, 확정 후 일정·금액 변경.
- 공고 수정·마감·삭제 허용 시점, 취소·불발·노쇼·분쟁 처리, 취소 후 실명·주소 접근.
- 당도 산식·변경 내역, AI 모델·예산·한도·외부 제공사 로그 보관 조건, 최근 검색·공고 임시저장 보관. 서비스 서버의 AI 대화 이력 미저장 방침은 확정이다.
- 신고·차단·탈퇴·고객지원·관리자 상세 경로와 Top 10 정책은 현재 완료된 기능으로 추가하지 않는다.

직접 지급·플랫폼 결제/대금 보관/정산/자동 환불 제외는 이미 확정된 경계다. 지급 방법·시점은 확정 당사자가 합의하므로 플랫폼이 정할 결제 정책으로 다시 열지 않는다.

## 7. 이번 작업 결과의 범위

이번 작업은 `PLAN.md` 작성과 기준 문서 대조다. `PLAN_원본.md`·IA·USER_FLOW·HTML은 보존한다. 이번 사용자 결정과 후속 상세 설계의 미정 항목은 6장에 명시했다.

코드 구현, 폴더 이동, DB 변경, 환경 설치, 외부 인증·API·AI 연동, 기능 테스트, 배포는 이번 작업에서 `NOT_RUN`이다. 기존 프로젝트 전체의 구현 상태를 새로 조사하거나 판정한 문서는 아니다.
