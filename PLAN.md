# HTML 유지·기존 Supabase 기반 백엔드 구조 설계

- 작성 기준: 2026-09-22 대화의 최신 결정과 설계안.
- 상태: 설계 문서 저장. 코드 구현·DB 변경·환경 구성·실제 연동·테스트 실행은 하지 않음.
- 참고 자료: [PRD](docs/planning/requirements/PRD.md), [IA](docs/planning/design/IA.md), [USER_FLOW](docs/planning/design/USER_FLOW.md), [와이어프레임 HTML](docs/planning/design/yumidang-wireframes.html).
- 문서와 최신 대화의 결정이 충돌하면 최신 확정사항을 우선한다. 아래의 목표 구조와 처리 방식은 구현 완료를 뜻하지 않는다.

## 1. 확정된 기준

이번 단계에서는 **폴더·데이터 경계·API 역할·AI 처리 흐름·검증 환경을 설계**한다. 이 문서 저장 외에 구현이나 기존 파일 수정은 진행하지 않는다.

- 현재 와이어프레임 HTML은 그대로 유지한다. React 전환이나 화면 재작성은 하지 않는다.
- 기존 Supabase와 DB 변경 이력은 유지한다.
- 기존 React 화면·프론트 연결 코드·테스트·실행 설정은 재사용하지 않는다.
- 이전 프로젝트 백업은 만들지 않는다.
- 검증 환경은 기존 원격 DB와 분리한 **로컬 Supabase**로 새로 구성한다.

이번에 추가로 확정한 서비스 기준은 다음과 같다.

| 항목 | 확정 기준 |
|---|---|
| PASS 연동 전 가입 | 이름·생년월일 직접 입력과 성인 여성 이용 조건 동의. 신원 검증 완료로 표시하지 않음 |
| PASS 연동 후 가입 | **PASS 한 번만** 수행. 인증된 이름·생년월일·성별로 이용 자격 확인 |
| 이후 로그인 | 휴대폰 인증번호 방식. 로그인마다 PASS를 요구하지 않음 |
| 공개 위치 | 등록 장소명·시구동·도로명과 건물번호까지 검색·표시 |
| 비공개 위치 | 층·좌석·구체적인 만남 안내는 확정 상대에게만 제공 |
| AI 대화 | 현재 탐색 중만 유지. 서버에 대화 이력 저장하지 않음 |
| 리뷰 요약 | 텍스트가 작성된 공개 후기 3개 이상부터 제공 |

## 2. 목표 폴더 구조

아래는 새 설계의 구성이다. 현재 생성하거나 이동하는 것은 아니다.

```text
backend/
├── README.md
├── contracts/                    # 화면↔백엔드 연결 규칙 설명
└── supabase/
    ├── config.toml               # 새 로컬 실행 설정
    ├── migrations/               # 기존 이력 유지, 후속 변경만 추가
    ├── seeds/                    # 새 가상 테스트 데이터
    └── functions/
        ├── signup/               # 가입·PASS 결과 검증·계정 연결
        ├── verification/         # 선택 이메일·1원 계좌 인증
        ├── places/               # 카카오 장소 검색 연결
        ├── event-sync/           # 공연·전시·축제 수집
        ├── ai-chat/              # 자연어 탐색 요청
        ├── review-summary-worker/ # 리뷰 요약 작업
        ├── scheduled-jobs/       # 시간 기반 처리·재시도
        └── _shared/
            ├── contracts/        # 실제 요청·응답 형식
            ├── auth/             # 로그인·회원 자격·접근 권한
            ├── services/         # 기능별 처리와 DB 호출
            ├── integrations/     # 외부 업체별 연결 코드
            ├── ai/
            │   ├── chatbot/
            │   ├── review-summary/
            │   └── providers/
            ├── jobs/             # 작업 등록·중복 방지·재시도
            └── observability/    # 오류·처리 시간·사용량 기록

tests/                            # 기존 테스트 코드는 재사용하지 않음
├── database/                     # DB 제약·접근 권한
├── functions/                    # 서버 함수
├── contracts/                    # 화면에 필요한 응답 형식
├── integration/                  # 여러 사용자 간 실제 처리
├── ai/                           # 검색·요약 평가
└── fixtures/                     # 개인정보 없는 검증 자료

tools/
└── local/                        # 새 실행·검증 명령과 환경 점검
```

각 영역의 책임은 다음처럼 나눈다.

- **DB:** 접근 권한, 양쪽 동의, 중복 신청·평가 방지, 칭찬 집계.
- **서버 함수:** 비밀키가 필요한 외부 인증·장소·행사·AI 연결.
- **AI:** 자연어 해석과 원문 요약. 회원 자격이나 매칭 확정을 판단하지 않음.
- **예약 작업:** 행사 갱신, 리뷰 공개 시점 처리, 요약 생성·재시도.

같은 규칙을 화면·서버·AI에 각각 복사하지 않고, 서버와 DB의 공통 처리 경로를 사용한다.

## 3. 기존 DB와 새 API의 연결 설계

### 가입·로그인

가입과 로그인은 분리한다.

> 가입: PASS → 서버에서 결과 검증 → 성인 여성 조건 확인 → 계정·세션 연결 → 사진 등록 → 가입 완료
>
> 로그인: 휴대폰 번호 → 인증번호 확인 → 기존 계정의 세션 시작

위 가입 흐름은 PASS 연동 이후의 목표다. 연동 전에는 1장의 본인 입력·동의 기준을 적용하며, 이를 PASS 인증 완료로 간주하지 않는다.

PASS 결과는 브라우저가 보낸 이름·성별을 그대로 믿지 않고, 서버가 인증 내역을 조회해 확인한다. [본인인증 연동 문서](https://developers.portone.io/opi/ko/extra/identity-verification/readme-v2)

PASS 가입 후 Supabase 세션을 발급하는 부분은 별도 연결 모듈로 격리한다. **가입 시 두 번째 인증을 요구하지 않는 것**을 완료 조건으로 두되, 구체적인 연결 방식은 실제 연동사 선정 후 기술 검증 대상으로 남긴다.

직접 입력한 정보, PASS로 확인한 정보, 로컬 테스트 정보를 구분한다. 기존 계정이나 테스트 데이터를 자동으로 ‘PASS 인증 완료’로 승격하지 않는다.

### 공고·검색·위치

공고 작성 요청부터 다음 정보를 분리한다.

- 공개: 제목, 장소명, 기본 주소, 일정, 동행 비용.
- 비공개: 층·좌석·구체적인 만남 안내.

일반 검색과 AI 검색은 동일한 공개 검색 기능을 사용한다. 비공개 상세 지점은 검색 대상·검색 응답·AI 입력에서 모두 제외한다.

기존 `exact_location`에는 공개 주소와 상세 안내가 섞여 있을 수 있으므로 **자동으로 공개 필드에 옮기지 않는다.** 새로 구분된 정보가 없는 기존 공고는 기존 공개 지역까지만 제공한다.

### 매칭·평가·알림

- 작성자의 상대 선택과 신청자의 최종 동의를 별도 요청으로 분리한다.
- 같은 일정·금액의 조건에 양쪽이 동의했는지 서버가 확인한다.
- 기존 단독 확정 기능이 우회 경로로 남지 않도록 후속 DB 변경을 설계한다.
- 유료 등록·신청·최종 확정에서는 필요한 계좌 인증 상태를 서버가 확인한다.
- 평가 요청은 경험 선택·별점·선택 칭찬·선택 한마디를 받는다. 제출 성공 후 수정 불가 규칙을 서버에서 적용한다.
- 공개된 평가의 칭찬 횟수와 AI 요약을 원문 리뷰와 구분해 제공한다.
- 알림은 최종 동의 요청·확정·평가 가능 상태를 구분한다.

기존 DB 이력을 덮어쓰거나 DB를 초기화하지 않고, 필요한 변경을 후속 이력으로 추가하는 방식이다.

## 4. AI 두 기능의 설계

| 구분 | AI 챗봇 | AI 리뷰 요약 |
|---|---|---|
| 실행 계기 | 사용자의 자연어 요청 | 공개 텍스트 후기의 추가·공개 상태 변경 |
| 입력 | 현재 대화, 탐색 조건, 허용된 프로필 성향 | 공개된 텍스트 후기 |
| 처리 | 조건 해석 → 필요 시 질문 → 공고·행사 검색 → 설명 | 원문 확인 → 요약 → 근거·공개 상태 재확인 |
| 출력 | 해석한 조건, 실제 결과 카드, 추천 이유·확인할 점 | 요약문, 기준 후기 수, 갱신 시각 |
| 실패 시 | 일반 탐색으로 이동 가능 | 원문 후기·칭찬 차트는 계속 제공 |
| 금지 | 임의 SQL, 신청·매칭·결제, 비공개 정보 조회 | 비공개 후기 사용, 원문에 없는 평가 생성 |

챗봇은 검색에서 받은 실제 공고·행사만 추천한다. ‘이번 주말 전시’에는 주말과 기간이 겹치는 장기 전시와 신규 전시를 함께 포함하며, 주변 반경 검색은 제공하지 않는다.

리뷰 요약은 프로필을 열 때마다 생성하지 않고 작업으로 처리해 저장한다. 후기가 비공개로 바뀌면 해당 내용을 포함한 요약도 노출을 중단하고 다시 처리한다.

추가 AI는 초기 필수 범위에 넣지 않는다. 신고 요약·운영자 검토 보조는 후속 후보이며, 칭찬 집계·매칭·알림·행사 수집은 일반 프로그램으로 처리한다.

## 5. 새 검증 환경과 남은 확인 사항

로컬 환경은 기존 DB 변경 이력과 새 가상 데이터로 구성한다. 실제 사용자 데이터 복사나 이전 프로젝트 백업은 하지 않는다. 로컬 Supabase에는 CLI와 컨테이너 실행 환경이 필요하다. [공식 안내](https://supabase.com/docs/guides/local-development)

새 테스트의 핵심 기준은 다음과 같다.

- 가입·PASS 확인·로그인·프로필 완료 상태가 혼동되지 않는지.
- 가입 시 PASS 외의 추가 인증을 요구하지 않고, 기존 회원 로그인은 휴대폰 코드로 가능한지.
- 작성자·확정 상대·제3자·비로그인의 정보 접근이 구분되는지.
- 기본 주소는 검색되지만 비공개 상세 지점은 검색·AI로 유출되지 않는지.
- 단독 확정·동시 확정·중복 평가를 서버가 막는지.
- 텍스트 후기 2개에서는 요약하지 않고 3개부터 처리하는지.
- AI 실패·재시도·비공개 전환이 기존 기능과 개인정보 보호를 깨뜨리지 않는지.

**아직 확정하지 않은 것:** PASS·문자·이메일·계좌 인증 제공사, PASS 가입의 세션 연결 방식, AI 모델·예산·한도, 취소·분쟁·결제 세부 정책이다. 해당 기능의 상세 설계에 들어갈 때 질문으로 결정하며, 임의의 기본값으로 운영 정책을 채우지 않는다.

현재까지 진행한 것은 자료·DB 변경 이력 확인과 구조 설계, 이 설계안의 문서 저장이다. 이 문서 작성 과정에서 코드·DB 변경, 환경 설치, 실제 연동, 테스트 실행은 하지 않았다.
