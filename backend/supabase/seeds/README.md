# 새 로컬 가상 데이터

담당: 이 안내와 로드 설정은 민규만 수정한다. 민규 데이터는 `minkyu/`, 종현 데이터는 `jonghyun/`에 각각 작성한다. 다른 담당의 파일을 수정하지 않는다.

실제 사용자 정보를 복사하지 않는다. 회원·공고·매칭·후기는 민규, 행사·AI·작업 사례는 종현이 작성한다. 데이터 파일명과 로드 순서는 로컬 환경 구성 시 확정한다. 기존 테스트 인증을 PASS 완료로 취급하지 않는다. 현재 seed SQL은 없으며 자동 실행하지 않는다.
