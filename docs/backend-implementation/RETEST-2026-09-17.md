# Supabase 구현 재검증 — 2026-09-17

## 후속 정책 구현 결과 (2026-09-17 10:56 KST 이후)

아래의 기존 “명세와의 차이” 1~3은 migration `20260917005621 retry_completion_dispute_review_policy`로 해소했다.

1. 철회 후에는 이전 행·메시지를 보존하고 새 request ID와 새 대화방을 생성한다. 이전 대화는 읽기 전용이며, 거절 이력이 있으면 `request_declined_history`로 재신청을 차단한다.
2. 종료 시각 이후 한 참여자의 최초 요청이 동행 전체를 즉시 완료한다. audit actor/time은 1행만 남고, `ends_at+24h` 미처리 건은 실제 pg_cron job이 자동 완료한다.
3. 완료 알림 뒤 24시간은 작성 가능·공개 보류 상태다. 양쪽 평가는 보류 종료 즉시, 단독 평가는 `completed_at+7일`에 공개한다. 분쟁 중 작성·공개·남은 시간은 동결된다.

검증: 기능 05/06/07/08/09 모두 PASS, 전체 브라우저 `run-20260917T010522-ay26` 17/17 PASS, 추가 공개 전이 브라우저 PASS. 자세한 실행 ID와 정책 fixture 결과는 [HARNESS-REPORT.md](HARNESS-REPORT.md)에 기록했다.

## 이전 기준선(아래 내용은 후속 정책 적용 전 기록)

- 검증 대상 커밋: `a449b05e3be513077a0fc756bd3815376d15d4bb`.
- 검증 시작 시 작업 트리는 깨끗했다. 제품 코드나 테스트 코드를 수정하지 않고 기존 하네스를 재실행했다.
- GPT/Codex 직접 재실행 결과이며, 별도로 설계한 독립 검증이나 Gemini 실행 결과는 아니다.
- 허용된 Supabase 프로젝트 `bndguguarijmghnkenvt`와 로컬 앱을 사용했다.
- 인증은 테스트 번호·고정 인증번호 방식이다. 실제 SMS 인증 성공을 의미하지 않는다.

## 실행 결과

| 검사 | 결과 |
|---|---|
| `npm test` | 95/95 PASS |
| `npm run build` | PASS. 내부 타입 검사(`tsc --noEmit`) 포함 |
| `git diff --check` | PASS |
| `npm run test:harness` | 103/103 PASS: 사전 검사 7, 기능별 검사 79, 브라우저 전체 흐름 17 |

- 실행 ID: `run-20260917T000709-1i5g`.
- 실행 시각: 2026-09-17 09:07:09~09:16:22 KST, 약 9분 13초(증거 타임스탬프 기준).
- 브라우저에서 공고 작성 → 신청 → 새로고침 없는 양방향 채팅 → 최종 확정 → 종료 후 양측 완료 → 블라인드 평가 및 공개 → 새로고침·재로그인 복원을 통과했다. 제3자·비로그인 사용자의 비공개 정보 접근 차단과 브라우저 미처리 오류 없음도 통과했다.
- [실행 증거](evidence/run-20260917T000709-1i5g/), [자동 생성 상태](STATE.md).
- 최초 실행 `run-20260917T000649-8m3y`는 샌드박스 네트워크 제한 상태에서 Auth 연결에 실패했다. 네트워크 접근을 허용한 재실행에서 원격 기능 검사는 통과했다. 최초 실패 증거도 보존했다.
- 빌드 경고: JavaScript 청크 860.58 kB, gzip 238.52 kB로 Vite의 500 kB 경고 기준을 초과한다. 빌드 실패는 아니다.
- 하네스가 실행 ID를 붙인 테스트 데이터와 테스트 계정을 생성했다. DB 초기화나 기존 사용자 삭제는 하지 않았다.

## 당시 명세와의 차이 / 후속 검토 (현재 해소됨)

PASS는 기존 테스트가 정의한 구현 계약에 대한 결과이며, 최신 노션 기능 전체와 일치한다는 판정이 아니다. 아래는 로컬 명세 문서와 현재 코드의 비교다. 이번 작업에서 노션 원문을 다시 조회하지 않았다.

1. **철회 후 재신청:** 로컬 인수인계 문서는 이전 이력을 보존하고 새 `request_id`를 생성하도록 정리되어 있다. 현재 `feature05_join_requests` 마이그레이션은 `(post_id, requester_id)` 고유 제약을 사용하며, 기존 테스트도 재신청으로 새 행이 생기지 않는 것을 통과 조건으로 삼는다. 명세에 맞춘 후속 수정이 필요하다.
2. **동행 완료:** 현재 구현은 종료 시각 이후 각 참여자가 완료를 확인하고, 두 번째 확인 시 동행이 완료된다. 질문지에는 요청자의 시작 이후 완료 처리와 예상 종료+24시간 자동완료 안이 남아 있다. 재확인 답변을 확정한 뒤 구현 기준을 맞춰야 한다.
3. **평가 공개:** 현재 구현은 양측 제출 시 공개한다. 로컬 명세의 '양측 제출 또는 평가기간 종료 시 공개'와 차이가 있다. 평가기간·분쟁 처리 관련 재확인 결론과 함께 정리해야 한다. 현재 기간 경계 테스트는 작성 가능 여부를 검증하며, 기간 종료 후 일방 평가 공개를 입증하지 않는다.

참조: 저장소 루트의 `노션_기능명세서_질문지_Claude_인수인계_2026-09-17.md`, `supabase/migrations/20260916105916_feature05_join_requests.sql`, `20260916110943_feature08_completion.sql`, `20260916111030_feature09_mutual_review.sql`.

## 이번 실행의 미검증 범위

- 실제 문자 수신 및 실서비스 인증 공급자 연동.
- 배포된 Vercel URL에서의 동작, 실제 사용자 사용성, 부하 테스트.
- 실제 7일 대기: 기간 경계는 서버 함수로 검사한다.
- 노션의 모든 기능·제재·초대 정책 구현 여부.
- 원격 마이그레이션 전체와 로컬 SQL의 동일성: 사전 검사의 파일명 확인만으로 이를 보장하지 않는다.

커밋·푸시·배포는 수행하지 않았다.
