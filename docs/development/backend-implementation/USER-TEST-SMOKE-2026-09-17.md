# 유저테스트 직전 로그인·주요 흐름 스모크 테스트

## 결론

- 최종 판정: **NO-GO**
- 이유: 우선 대상 운영 URL `https://yumidang6.vercel.app`이 앱 대신 Vercel `404: NOT_FOUND`를 반환했다. 로그인과 공고 흐름을 운영 환경에서 시작할 수 없다.
- 비교 결과: 현재 로컬 소스를 원격 Supabase에 연결한 모바일 브라우저에서는 지정 계정 로그인, 6개 공고, 주요 읽기 전용 흐름, 로그아웃·재로그인이 통과했다. 로컬 통과를 운영 통과로 간주하지 않는다.
- 이 판정은 유저테스트 직전의 기술적 준비 상태만 뜻하며 사용자 가치나 실제 유저테스트 성공을 증명하지 않는다.

## 실행 정보

- 실행 시각: 2026-09-17 12:20~12:33 KST
- 운영 대상: `https://yumidang6.vercel.app`
- 로컬 비교 대상: `http://127.0.0.1:3000` (현재 Vite 소스 + 원격 Supabase)
- 모바일 브라우저: Chromium headless, 새 컨텍스트, 저장 상태 없음, `390 × 844`
- Git branch / commit: `main` / `904c4d2839709cd64e7f665e5acface03fea26ea`
- 시작 작업 트리: clean
- Supabase project ref: `bndguguarijmghnkenvt` (`https://bndguguarijmghnkenvt.supabase.co`)
- 프로젝트 확인: 프로젝트 고정 MCP의 API URL을 직접 조회해 ref 일치를 확인했다.
- 제품 코드·마이그레이션·환경 변수·Edge Function 설정은 변경하지 않았다. 커밋·푸시·배포도 하지 않았다.

## 데이터 비변경 사전 확인

| 항목 | 기대 | 실제 | 판정 |
|---|---:|---:|---|
| Auth 사용자 | 1 | 1 | PASS |
| 프로필 | 1 | 1 | PASS |
| 보존 대상 공고 | 6 | 6, 제목 6종이 각각 1개 | PASS |
| 공고 상태 | 모두 recruiting | 6/6 recruiting | PASS |
| 모집 마감·시작 시각 | 모두 미래 | 6/6 미래 | PASS |
| 비공개 장소 행 | 공고별 1개 | 6개 공고에 각각 1개 | PASS |
| Auth 테스트 인증 메타데이터 | true | 유일 계정에서 true | PASS |
| 테스트 인증 Edge Function | 활성 | 요청 전용 preflight가 HTTP 200 | PASS |

일반 Supabase 관리 앱의 Edge Function 목록 조회는 권한 부족으로 직접 확인하지 못했다. 대신 프로젝트 고정 연결, Auth health, 데이터 생성 없는 `request + login` preflight와 실제 브라우저 로그인을 통해 활성 상태를 확인했다.

## 코드 검사

| 명령 | 결과 | 비고 |
|---|---|---|
| `npm run lint` | PASS | TypeScript 오류 없음 |
| `npm test` | PASS | 96/96 |
| `npm run build` | PASS | 865.60 kB JS 청크 경고만 있음 |
| `npm run harness:preflight` | PASS | 7/7. 첫 샌드박스 실행은 외부 네트워크 차단으로 Auth health에서 실패했고, 네트워크 허용 재실행은 전 단계 통과 |

`harness:preflight`는 실행 전에 코드를 확인했다. 존재하지 않는 번호에 `request`와 `mode: login`만 보내며 계정 생성·OTP 검증·프로필 쓰기를 하지 않는다. 계정과 공고를 추가하는 다른 하네스 명령은 실행하지 않았다.

## A. 비로그인 진입

### PRODUCTION — BLOCKED

- 입력 전제: 저장 로그인 상태가 없는 새 모바일 컨텍스트.
- 수행 행동: 운영 URL 루트 진입.
- 기대 결과: 유미당 앱과 공개 공고 목록 표시.
- 실제 관찰: Vercel `404: NOT_FOUND`, 제목 `This page doesn’t exist`, HTTP 404. 앱 DOM이 없어 이후 항목을 수행할 수 없었다.
- 판정: **BLOCKED**. 운영 배포 진입 실패는 최종 `NO-GO` 사유다.

### LOCAL — PASS

- 앱이 흰 화면이나 무한 로딩 없이 표시됐다.
- 공개 목록은 정확히 6개였고 지정된 6개 제목이 각각 한 번 표시됐다. 그 외 공개 공고는 없었다.
- 공고 상세에서 제목, 카테고리, 공개 지역, 일정, 설명이 표시됐다.
- 비로그인 상세의 정확한 장소는 `1:1 매칭 확정 후 공개`와 비공개 잠금 상태로 표시됐다.
- 공개 목록과 상세에서 원본 실명, 전체 전화번호, 정확한 생년월일이 노출되지 않았다.
- 개인정보가 포함될 수 있는 스크린샷은 생성하지 않았다.

## B. 검색과 탐색

### PRODUCTION — BLOCKED

운영 루트 404로 검색 UI에 진입할 수 없었다.

### LOCAL — PASS

| 행동 | 기대 결과 | 실제 관찰 | 판정 |
|---|---|---|---|
| `성수` 검색 | 성수 팝업 또는 서울숲 공고 표시 | 관련 공고 2건 표시 | PASS |
| `식사` 카테고리 | 홍대 돈까스와 망원시장 공고 표시 | 두 공고가 포함된 2건 표시 | PASS |
| 필터 초기화 | 전체 6개 복귀 | 6건 복귀 | PASS |
| 상세 닫기 | 이전 목록 상태 유지 | `성수` 검색어와 결과 상태 유지 | PASS |

## C. 실제 테스트 로그인

### PRODUCTION — BLOCKED

운영 루트 404로 로그인 화면에 진입할 수 없었다.

### LOCAL — PASS

- 입력 전제: 새 브라우저 컨텍스트, 로그인 모드, 지정 계정(보고서에는 끝 4자리만 `5555`로 식별).
- 수행 행동: 번호 입력 → 인증번호 요청 → 화면의 테스트 OTP 입력 → Supabase 인증 확인.
- 기대 결과: 실제 SMS 없이 실제 Supabase 세션 생성, 기존 프로필 복구, 새로고침 유지.
- 실제 관찰:
  - 로그인 모드와 테스트 인증 안내가 표시됐다.
  - 요청 뒤 `실제 문자는 보내지 않아요` 안내와 테스트 OTP 입력 UI가 표시됐다.
  - 인증 성공 후 로그인 모달이 닫혔고 기존 프로필을 사용했다. 추가 회원가입·기본 프로필 입력으로 이동하지 않았다.
  - 새로고침 뒤 로그인과 사용자 상태가 유지됐다.
- 판정: **PASS**.

실제 SMS 미발송 근거는 세 가지다. 화면이 요청 전후 모두 실제 문자를 보내지 않는 테스트 인증임을 명시했고, 현재 Edge Function의 비레거시 `request` 구현은 Auth/SMS 호출 없이 안내만 반환하며, 브라우저는 같은 화면에서 고정 테스트 OTP로 실제 Supabase 세션을 만들었다.

## D. 로그인 후 주요 흐름

### PRODUCTION — BLOCKED

운영 로그인 자체가 불가능해 수행하지 못했다.

### LOCAL — PASS

- Me 화면에 본인 프로필 카드가 표시됐다. 공개 영역에는 전체 전화번호와 정확한 생년월일이 표시되지 않았다.
- `내가 쓴 공고`에 6개가 중복 없이 표시됐고 지정 제목이 모두 일치했다.
- 작성자 상태의 공고 상세에서는 일반 비로그인 잠금 문구가 아닌 작성자 전용 정확한 장소 영역을 확인했다. 장소 원문은 수집하거나 보고서에 저장하지 않았다.
- 채팅, 알림, 상태별 동행은 데이터가 없는 상태에서 오류 없이 빈 상태를 표시했다.
- 모바일 하단 내비게이션, 상세·알림 모달 닫기, 화면 전환과 스크롤을 사용할 수 있었다.
- 공고 작성·수정·삭제·신청, 채팅 전송, 매칭, 완료, 평가 동작은 누르지 않았다.

## E. 로그아웃과 재로그인

### PRODUCTION — BLOCKED

운영 로그인 불가로 수행하지 못했다.

### LOCAL — PASS

- 로그아웃 후 새로고침했을 때 로그인 전 상태였다.
- 같은 지정 계정으로 다시 로그인했다.
- 재로그인 후 같은 6개 공고가 유지됐다.
- 최종 읽기 전용 DB 확인에서 Auth/프로필/공고 수가 `1 / 1 / 6`이었다.

## 콘솔·네트워크

- PRODUCTION: 루트 문서 HTTP 404와 이에 대응하는 브라우저 리소스 오류 1건.
- LOCAL: 처리되지 않은 콘솔 오류와 page error 없음.
- LOCAL: 로그아웃 요청 1건이 브라우저에서 `ERR_ABORTED`로 기록됐다. 다만 로그아웃 뒤 새로고침에서 익명 상태가 확인되고 같은 계정 재로그인도 성공해 기능 실패로 이어지지 않았다.
- 토큰, API 비밀키, 전체 전화번호, 생년월일, 정확한 장소는 콘솔 증거와 보고서에 저장하지 않았다.

## 점검 후 공고 정리

- 삭제 전 전체 공고: 6
- 보호 조건: PASS
  - Auth 사용자 정확히 1명
  - 유일 프로필이 지정된 보호 속성과 일치
  - 보존 제목 6개가 모두 존재
  - 6개가 동일 작성자 소유
  - 제목별 최신 1개를 보호 대상으로 선택
- 읽기 전용 삭제 후보: 0개
- 보호 조건부 `DELETE` 실행 결과: 0개 삭제
- 최종 전체 공고: 6
- 최종 검증: 제목별 1개, 동일 보호 계정 소유, 비공개 상세 각 1개, 모두 recruiting, 모집 마감·시작 시각 미래
- Auth/프로필 최종 수: `1 / 1`
- 신청/채팅/동행/평가/Storage 최종 수: `0 / 0 / 0 / 0 / 0`

삭제 후보가 이미 없었기 때문에 실제 제거된 데이터와 연쇄 삭제는 없다.

## NOT_RUN 범위

다음은 Auth 계정이 1개뿐이므로 실패가 아니라 `NOT_RUN`이다.

- 다른 사용자의 공개 프로필 조회
- 공고 신청·철회·재신청
- 작성자의 신청 수락·거절
- 1:1 채팅과 Realtime 양방향 수신
- 최종 매칭
- 동행 완료·이의 제기
- 블라인드 상호 평가와 평가 공개

추가로 데스크톱 재현은 모바일 로컬 화면이 깨지지 않아 실행하지 않았다. 운영 A~E는 루트 404 때문에 A에서 차단됐으며 로컬 결과로 대체하지 않았다.

## 가장 중요한 실패와 최소 후속 조치

- 우선순위: **P0 — 운영 진입 불가**
- 재현:
  1. 새 브라우저 컨텍스트에서 `https://yumidang6.vercel.app`을 연다.
  2. 유미당 앱 대신 Vercel `404: NOT_FOUND`가 표시되는 것을 확인한다.
- 가장 작은 후속 조치: 해당 운영 도메인을 현재 유효한 Vercel 배포에 다시 연결하거나 올바른 운영 배포를 복구한 뒤, 같은 URL에서 A~E 전체를 재실행한다. 이번 점검에서는 배포·도메인 변경을 수행하지 않았다.

