# 유미당 인수인계 — 2026-09-18 (최신)

작업 폴더: `/Users/b/Documents/Antigravity/yumidang`
작성: Claude Code 세션 (2026-09-18 새벽 KST)

> **이 파일 하나만 읽고 이어서 작업할 수 있게 쓴 문서다.** 이전 인수인계는 `docs/handoffs/CLAUDE_CODE_UT_개선_후속_인수인계_2026-09-17.md`(역사 기록)이며, 그 문서의 승인 대기 항목(인덱스·commit·push·배포)은 **모두 처리됐다.**
> 규칙: 작업을 수행할 때마다 이 폴더에 인수인계를 갱신하거나 새 날짜 파일을 남긴다(사용자 지시, 2026-09-18).

---

## 1. 지금 상태 요약

| 영역 | 상태 |
|---|---|
| 코드 | `main` = `origin/main` = **`76a4952`**, 작업 트리 깨끗(문서 작업분 제외) |
| 배포 | Vercel Production이 `76a4952`로 자동 배포 완료(Ready), `https://yumidang.vercel.app` |
| DB | Supabase `bndguguarijmghnkenvt`. UT 알림 기능 + FK 인덱스까지 적용 완료 |
| 데이터 | **회원·공고·채팅·약속·평가·프로필 사진 전부 삭제됨(0건)**. 스키마·권한·bucket은 유지 |
| 다음 큰 작업 | ① 남은 정책 결정(GPT) ② UX/UI 전면 교체(Stitch→Antigravity) ③ 주소·행사 외부 API 연동 |

### 1.1 2026-09-17~18에 실제로 한 일

1. Supabase 후속 인덱스 `20260917141449_notifications_join_request_index` 적용(Codex 실행, Claude가 원격 읽기 전용 교차 확인). 인덱스 valid/ready, FK 미인덱스 경고 해소, 새 `unused_index` INFO는 사용 통계가 없어서 뜬 것이라 유지.
2. commit 3개 → push → Vercel 자동 배포. 배포 전 검사 직접 실행: `npm run lint` PASS, `npm test` 118/118 PASS, `npm run build` PASS, `git diff --check` PASS. 공개 번들의 Supabase URL이 `bndguguarijmghnkenvt`인 것과 테스트 전화번호 미노출을 확인.
3. **사용자 요청으로 전체 데이터 삭제.** 트랜잭션 1회로 `notifications → 분쟁/평가/완료확인 → chat_messages → appointments → join_requests → post_private_details → posts → private.* → profiles → auth.users` 순서 삭제, 프로필 사진 5개는 Storage API로 삭제(직접 SQL 삭제는 Supabase가 차단). 남긴 것: 테이블·RLS·RPC·migration 기록·`profile-images` bucket.
4. UT 후속 기획 정리: 디자인 시스템 추출, 화면 인벤토리 조사, Stitch 프롬프트·Antigravity 지시문 작성, 정책 프롬프트 보강, 외부 API 조사.

---

## 2. 고정 대상 (바꾸지 말 것)

- Git: `https://github.com/jjackbb/yumidang.git`, 브랜치 `main`
- Supabase: `bndguguarijmghnkenvt` **하나만**
- Vercel: `jjackbb-projects/yumidang` **하나만**, Production `https://yumidang.vercel.app`
- Vercel은 **GitHub 연동 자동 배포**다. `main`에 push하면 곧바로 Production이 바뀐다. → **push 승인 = 배포 승인**임을 사용자에게 미리 알리고 받는다.
- Vercel MCP는 403이라 CLI(`npx vercel@latest ... --scope jjackbb-projects`)로 조회한다. GitHub MCP는 인증 실패 상태.
- 로컬 개발 서버가 3000 포트에 떠 있을 수 있다(실제 Supabase에 연결됨, 화면 조작 시 실데이터 생성).

---

## 3. 작업 원칙 (사용자 지시)

1. 한국어로 보고한다. 사용자는 개발자가 아닌 기획자다. 결론과 이유를 먼저 쓴다.
2. 원격 변경(DB 적용, push, 배포, 데이터 삭제)은 **단계마다 별도 승인**을 받는다. 한 단계의 승인을 다음 단계로 확대하지 않는다.
3. 사용자의 기존 변경·문서·증거를 임의로 되돌리거나 지우지 않는다. DB는 forward-only migration.
4. PASS/FAIL/NOT_RUN을 구분해 보고하고, 과거 실행 결과를 현재 검증처럼 쓰지 않는다.
5. 정책·제품 결정은 사용자(또는 GPT 정책 세션) 몫이다. Claude가 새 정책을 만들지 않는다.
6. **작업이 끝나면 인수인계를 남긴다.**

---

## 4. 도구 분담 (사용량 배분)

| 도구 | 담당 | 이유 |
|---|---|---|
| GPT Plus (주간 30% 잔여) | 남은 정책 질문 29개 + 추가 안건 결정 대화 | 코드 없이 문서만 다루는 긴 대화 |
| Antigravity (Gemini Pro) | Stitch 시안을 코드 화면에 적용(분량 큰 반복 작업) | 저장소·브라우저 접근 가능 |
| Google Stitch | 화면 시안 생성 | 별도 무료 한도 |
| Claude Pro | 프롬프트·지시문 작성, 화면 검수, 원인 불명 버그, 원격 작업 | 사용량 아끼기 위해 핵심만 |

충돌 방지: **코드는 한 번에 한 도구만 수정**한다. 도구를 바꾸기 전 commit한다. 정책 트랙은 문서만 수정한다.

---

## 5. UX/UI 교체 계획

작성된 문서:

- `docs/planning/design/DESIGN.md` — 홈 화면에서 추출한 디자인 시스템(색·글꼴·radius·컴포넌트). Stitch 첫 입력용.
- `docs/planning/design/STITCH_PROMPTS.md` — 화면별 Stitch 프롬프트(S01~S19, 1·2·3차와 보류 목록 포함).
- `docs/planning/design/AI_FEATURES_DESIGN.md` — AI 브리핑·공고 찾기 챗봇 설계안(구현 전).
- `docs/planning/design/ANTIGRAVITY_UI_APPLY_PROMPT.md` — 시안을 코드에 입히는 실행 프롬프트(로직 불변, 테스트 통과 후 화면 단위 commit).

순서: 1차 S01~S09-3(정책 영향 적음) → 2차 S10~S15-2 → 3차 S16~S19(정책 확정 후). 홈 화면은 교체 대상이 아니다(디자인 기준).

진행 방법: Stitch에 DESIGN.md 입력 → 화면 프롬프트 → 결과를 `docs/planning/design/stitch-export/`에 저장 → Antigravity가 한 화면씩 적용 → 화면 2~3개마다 Claude 검수.

---

## 6. 사용자 확정 결정 (다시 묻지 말 것)

`docs/prompts/02_남은_정책_재확인_진행_프롬프트_2026-09-17.md` 7-2에도 기록돼 있다.

| ID | 결정 |
|---|---|
| D-A1 | 공고 카테고리 12개 **필수 유지**(1단계) |
| D-A2 | 공고 장소는 주소 검색으로 정확히 입력, **공개는 시·구·동까지**, 정확한 주소는 매칭 확정 상대만 |
| D-A3 | 모집 마감 유지 + 일정 입력을 **아이폰 캘린더 방식** UX로 |
| D-A4 | 상대 연령은 **나이 범위 슬라이더(18~120세)** + 숫자 입력, 드래그 중 값 즉시 표시 |
| D-A5 | 태그·"파트너에게 바라는 점" 유지 |
| D-A6 | 예시 행사 → **실제 행사 데이터**로 교체 |
| D-A7 | 행사 분류는 **팝업/전시/축제/공연 4개만** |
| D-A8 | 가입 후 프로필 설정에서 **취향과 활동·대화 방식·MBTI** 입력 |
| D-A9 | **Ai 브리핑 도입**, API 연동 전 UI 먼저 |
| D-A10 | 후기는 새 시안(긍정 후기 공개·태그·리뷰 신고)로, 단 세부는 재확인 |
| D-A11 | 마스킹 이름 + 만 나이 유지, 단 **표기는 "만" 없이 "25세"** |
| D-A12 | 당도 기본값 **15**, 표기 **"15당"** |
| **D-A13** | 주소·장소 검색은 **카카오 우편번호 서비스 + 카카오 로컬 API**(장소명 검색까지). 네이버는 보류 |
| **D-A14** | 행사 데이터는 **무료인 범위에서 KOPIS + 서울 열린데이터광장 문화행사정보 + 한국관광공사 TourAPI 전부** 연동. 팝업은 공개 API가 없어 운영자 등록 |
| **D-A15** | **AI 기능 2종 도입**: ① 후기 요약 Ai 브리핑 ② 자연어 공고 찾기 챗봇. 테스트는 **포텐스닷 API**, 이후 **제미나이 API**로 전환. 설계안 `docs/planning/design/AI_FEATURES_DESIGN.md`, 구현 전 승인 필요 |
| **D-A16** | Ai 브리핑 캡션 문구를 **"사용자 리뷰를 바탕으로 동행 성향을 요약했습니다."** 로 한다(2026-09-20). 기존 "공개된 후기 N건 기준 · 안전을 보증하는 정보는 아니에요"는 사용하지 않는다. 단어는 특징/취향/성향 중 **성향** 추천 |

---

## 7. 미결정·확인 필요

### 7.1 정책 (GPT 트랙에서 결정)

- 남은 정책 질문 **29개**: `docs/prompts/02_…md` 2절 목록.
- 추가 안건 7-1: "준비 중"만 뜨는 기능(신고·차단·초대·관심친구·공고 수정/마감/삭제·확정 취소·KYC·유료 결제·안심 통화) 숨길지/표시할지, 신고 화면 문구 충돌, 에스크로 예시 데이터 노출.
- 추가 안건 7-3: 후기 공개 기준, **D-A16으로 삭제된 "안전 보증 아님" 안내를 다른 화면에서 보완할지**, 나이 하한 18 vs 가입 19세, "25세" 표기 오해, 당도 증감 규칙, Ai 브리핑 연동 전 표시, MBTI 필수 여부, 이벤트 선택 범위.
- 결정 기록 파일: `docs/planning/product-decisions/정책_결정_진행기록_2026-09-17.md`(다른 세션이 생성함, Claude는 건드리지 않았다).

### 7.2 구현 전 준비 (사용자만 가능)

발급이 필요한 키. 모두 무료 범위로 확인했으나 한도·이용조건은 발급 시 약관에서 재확인한다.

| 용도 | 발급처 | 비고 |
|---|---|---|
| 장소명 검색 | Kakao Developers REST API 키 | 우편번호 서비스는 키 불필요 |
| 공연 | KOPIS Open API 서비스키 | 장르코드·이미지 권리·호출 한도 확인 필요 |
| 전시·축제(서울) | 서울 열린데이터광장 인증키 | 공공누리 1유형, 출처 표시 조건 |
| 축제(전국) | 공공데이터포털(data.go.kr) TourAPI 키 | 개발계정 일 1,000건 |

---

## 8. 외부 API 조사 결과 (2026-09-18)

### 8.1 주소·장소 검색 → D-A13

- **카카오 우편번호 서비스**: 무료, 키 불필요, 사용량 제한 없음, 스크립트만 넣으면 됨. 응답에 시·도/시·군·구/법정동·행정동이 분리돼 있어 **공개용 시·구·동 추출에 적합**. 하단 "Powered by kakao" 로고를 지우면 안 된다.
- **카카오 로컬 API(키워드 검색)**: "여의도 한강공원" 같은 **장소명 검색 가능**. REST 키가 필요하므로 **Supabase Edge Function 프록시로 호출**해 키 노출을 막는다. 검색 결과 대량 저장은 제한이 있으므로 **사용자가 최종 선택한 1건만 저장**한다(정확한 문구는 카카오 문의로 확인 필요).
- 네이버: 지도 API가 2025년 신규 신청 차단·무료 이용량 종료 공지 이력이 있어 현재 불확실(확인 필요). 지역검색 API도 서버 프록시 필요, 행정구역 구조화 필드 제공 여부 확인 필요 → **보류**.

### 8.2 행사 데이터 → D-A14

| 분류 | 출처 | 조건 |
|---|---|---|
| 공연(+장르 Top 10) | KOPIS Open API(공연목록·상세·예매상황판) | 키 신청. 응답 XML 추정, 장르코드·상업적 이용·이미지 권리 **확인 필요** |
| 전시·축제(서울) | 서울 열린데이터광장 `culturalEventInfo` | 공공누리 1유형(출처표시 시 상업적 이용 가능), 매일 갱신, 분류·자치구·장소·기간·요금·이미지·링크 제공 |
| 축제(전국) | 공공데이터포털 TourAPI `searchFestival` | 공공누리 1·3유형, 개발계정 일 1,000건 |
| 팝업 | **공개 API 없음** | 운영자 수동 등록 권장. 네이버 검색 결과 자동 수집·저장은 약관 위반 위험 |

권장 구조: 하루 1회 Edge Function(cron)이 각 출처를 가져와 `events` 테이블에 외부 ID 기준 upsert → 이미지 라이선스 확인 후 Storage 캐시 또는 핫링크 → 화면에 "출처: ○○ · 갱신 시각" 고정 표기.

---

## 9. 다음에 할 일 (제안 순서)

1. **정책 트랙(GPT):** `02_…md` 전체를 넣고 3문항씩 진행 → 결정 기록 갱신.
2. **UI 트랙(Stitch→Antigravity):** S01부터 시안 생성·적용. 2~3화면마다 Claude 검수.
3. **API 트랙:** 키 발급 후 (a) 주소 검색 UI + 공개/비공개 분리 저장, (b) `events` 테이블 설계와 동기화 Edge Function, (c) 행사 화면 연결. 모두 migration은 forward-only, 적용 전 승인.
4. **AI 트랙:** `docs/planning/design/AI_FEATURES_DESIGN.md` 6절 결정 → P0(UI 더미) → 포텐스닷 어댑터 → 제미나이 전환. 포텐스닷 API 스펙은 미확인.
5. 데이터가 비어 있으므로, 배포 후 화면 점검 12개 항목은 **새 계정으로 가입해 재실행**해야 한다(현재 NOT_RUN).

---

## 10. 알려진 위험

- Production과 개발 서버 모두 **실제 Supabase**를 본다. 화면 조작이 곧 실데이터다.
- push 즉시 배포되므로 되돌리려면 재배포가 필요하다.
- 500KB 초과 번들 경고가 계속 남아 있다(빌드 실패 아님).
- 공개 번들에 데모용 예시 인증번호(`123456`, `246810`)가 남아 있다. 실서비스 로그인과 무관하지만 UT 혼동 요인이 될 수 있다.
- 과거 프롬프트 문서(`docs/archive/`)에 전화번호 원문 2개가 남아 있다. 기존 commit에 포함된 내용이라 새 노출은 아니지만 정리 여부 결정이 필요하다.
- `.DS_Store`가 추적되고 있고 `dist/`는 무시 설정인데도 일부 파일이 추적 중이다. 정리하려면 사용자 확인이 필요하다.
- 작업 트리에 다른 도구(GPT/Antigravity)의 변경이 섞일 수 있다. 시작 전 `git status`로 확인한다.
