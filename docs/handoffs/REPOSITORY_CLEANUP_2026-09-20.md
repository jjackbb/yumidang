# 폴더 정리·커밋/푸시 인수인계 — 2026-09-20

## 사용자 지시와 다음 작업

사용자가 설치·생성 파일의 Git 추적 정리, 최신 인수인계 중심 시작 안내, 폴더 최적화, 이번 변경의 커밋·푸시를 요청했다. `main` push가 Vercel 운영 배포로 이어짐을 알렸다.

팀원 1·2에게는 **내일(2026-09-21) 전달해 작업을 시작할 예정**이다. 아직 전달·실행 완료로 기록하지 않는다. 사용자와 GPT는 이후 팀 합의용 전체 화면 구조를 설계한다. [역할 분담](../work-allocation/README.md), [화면 목록](../planning/design/SCREEN_INVENTORY.md)을 따른다.

## 정리 범위

- `node_modules/` 9,034개, `dist/` 3개, `.vercel/` 2개, 루트 `.DS_Store` 1개: **총 9,040개 Git 추적 해제**.
- 설치·빌드·Vercel 연결 파일은 로컬에 유지. 루트와 docs의 Finder `.DS_Store`는 삭제.
- `.gitignore`의 생성물·환경파일·캐시 범위를 설명하고 TypeScript 증분 빌드 정보를 제외.
- 루트 README/HANDOFF는 짧은 최신 시작 안내로 교체. 이전 본문·로드맵·생성 도구 메타데이터는 [보관함](../archive/project-history/2026-09-20/README.md)에 보존.
- 완료된 UT 개선 프롬프트를 `archive/completed-prompts/2026-09-17/`로 이동하고 진입 링크 수정.
- [문서 지도](../README.md), [과거 자동화 도구 설명](../../scripts/README.md) 추가. 기존 검증 증거와 스크립트의 경로 의존성은 보존.
- 이전 작업의 미커밋 설계·정책·분담·인수인계 문서를 함께 버전 관리한다. 이미 삭제된 발표 프롬프트는 복구하지 않는다.
- 앱 소스·DB·제품 정책·실제 API 설정은 변경하지 않는다. 불필요한 패키지 제거·코드 분리 등은 이번에 하지 않는다.

## 저장 용량과 이력의 의미

설치 파일 약 214MB를 현재 Git 파일 목록에서 제외한다. 과거 커밋의 파일은 남으므로 원격 저장소 전체 용량이나 과거 이력 다운로드가 즉시 그만큼 줄어드는 것은 아니다. 강제 푸시·이력 재작성은 하지 않는다. 로컬 디스크 절약을 위해 개발에 필요한 `node_modules`를 지우지 않는다.

## 검증 상태

2026-09-20 로컬 검증:

- PASS: 스테이징된 파일만 임시 폴더로 내보낸 뒤 기존 `node_modules` 없이 `npm ci`로 226개 패키지 설치.
- PASS: 위 깨끗한 사본에서 테스트 118/118, TypeScript 검사, Vite Production build.
- PASS: 현재 안내·설계·인수인계·보관 문서 상대 링크 100개, 끊어진 링크 0개.
- PASS: Git 추적 중인 무시 파일 0개. 정리 후 추적 파일 572개(시작 시 9,595개).
- PASS: 앱 소스·이미지·DB·테스트·패키지·빌드 설정은 기준 HEAD 대비 변경 없음. 로컬 설치·빌드·Vercel 연결·환경 파일 유지.
- PASS: `git diff --check`. 기존 단일 JavaScript 번들 500KB 초과 경고는 남아 있으며 빌드 실패는 아니다.
- NOT_RUN: 운영 데이터를 쓰는 원격 하네스, 신규 실제 사용자 테스트. 문서·추적 정리에 필요한 검사와 구분한다.

## 커밋·푸시 기록 범위

작업 시작 시 로컬 HEAD와 실제 원격 main 모두 `76a49526807e7c87142ddb16b4915c25b8273e0a`였다. 대상은 `jjackbb/yumidang`의 `main`, Vercel `jjackbb-projects/yumidang`이다.

이 문서는 커밋에 포함될 준비·검증 기록이다. 이후 push와 자동 배포의 성공은 실제 원격 SHA·Vercel 상태·공개 URL로 확인해 사용자에게 보고한다. 이 파일의 존재만으로 배포 성공을 주장하지 않는다.
